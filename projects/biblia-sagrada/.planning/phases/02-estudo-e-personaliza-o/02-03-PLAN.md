---
phase: 02-estudo-e-personaliza-o
plan: 03
type: execute
wave: 3
depends_on: [02-01, 02-02]
files_modified:
  - src/lib/bible.ts
  - src/components/verse-actions.tsx
  - src/components/reader.tsx
  - data/raw/hymns.json
  - scripts/study-build.mjs
  - src/lib/study.ts
  - src/components/study-view.tsx
autonomous: true
requirements: [PER-01, PER-02, PER-03, EST-05]
estimate:
  tokens: 65000
  raw_tokens: 65000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "UsuÃ¡rio marca versÃ­culos com 5 cores fixas, aplicadas inline e persistidas no store study (sync-ready Fase 3) [PER-01, D-06, D-07]"
    - "UsuÃ¡rio cria e vÃª anotaÃ§Ãµes por versÃ­culo (painel de aÃ§Ã£o + lista na aba Estudo) [PER-02]"
    - "UsuÃ¡rio acessa hinÃ¡rios com letra completa (~50 hinos, mÃ­nimo aceito 30, todos domÃ­nio pÃºblico verificado) [PER-03, D-09, A1]"
    - "UsuÃ¡rio acompanha 2 planos de leitura fixos (BÃ­blia em 1 ano, NT em 90 dias) com progresso persistido no IDB [EST-05, D-10]"
    - "Progresso de plano de leitura nunca Ã© marcado automaticamente â€” sÃ³ por aÃ§Ã£o explÃ­cita do usuÃ¡rio [EST-05]"
    - statement: "ConteÃºdo de hinÃ¡rio (â‰¥30 hinos PD) e planos (2 gerados do index.json) sÃ£o produzidos nesta fase e versionados pelo pipeline [A1/OQ-1]"
      verification: backstop
  artifacts:
    - path: "src/components/verse-actions.tsx"
      provides: "Bottom sheet de aÃ§Ã£o por versÃ­culo: 5 cores + anotaÃ§Ã£o + remover (padrÃ£o book-picker)"
      min_lines: 40
    - path: "src/lib/bible.ts"
      provides: "StudyRecord CRUD (putStudyRecord/getStudyRecords/getAllStudyRecords/deleteStudyRecord) + PlanProgress (getPlanProgress/setPlanProgress)"
      contains: "putStudyRecord"
    - path: "src/components/reader.tsx"
      provides: "VersÃ­culos clicÃ¡veis com destaque inline (cor/nota) e abertura do verse-actions"
      contains: "getStudyRecords"
    - path: "data/raw/hymns.json"
      provides: "~50 hinos {id, title, verses: string[]} â€” domÃ­nio pÃºblico"
      min_lines: 20
    - path: "scripts/study-build.mjs"
      provides: "Estende com hymns.json + gera plans.json algoritmicamente do index.json"
      contains: "plans"
    - path: "src/lib/study.ts"
      provides: "getHymns()/getPlans() + tipos Hymn/Plan/DailyReading"
      contains: "getHymns"
    - path: "src/components/study-view.tsx"
      provides: "Abas Hinos, Notas e Planos (expansÃ£o do 02-02)"
      contains: "Notas"
    - path: "public/data/study/hymns.json"
      provides: "HinÃ¡rio precached"
      min_lines: 1
    - path: "public/data/study/plans.json"
      provides: "2 planos de leitura precached"
      min_lines: 1
  key_links:
    - from: "src/components/reader.tsx"
      to: "src/lib/bible.ts"
      via: "getStudyRecords(version, book, chapter) no efeito de capÃ­tulo + reload apÃ³s onChanged (Pitfall 3 â€” cores via style inline, nunca classes dinÃ¢micas)"
      pattern: "getStudyRecords"
    - from: "src/components/verse-actions.tsx"
      to: "src/lib/bible.ts"
      via: "putStudyRecord/deleteStudyRecord â€” id `${version}:${book}:${chapter}:${verse}` (D-06), updatedAt em cada escrita (Fase 3 sync)"
      pattern: "putStudyRecord"
    - from: "src/components/study-view.tsx"
      to: "URL"
      via: "onNavigate(book, chapter) â†’ ?b=&c= via replaceState string (Pitfall 4)"
      pattern: "replaceState"
    - from: "scripts/study-build.mjs"
      to: "public/data/index.json"
      via: "gera plans.json do Ã­ndice (66 livros com chapters) â€” sem duplicar contagem"
      pattern: "index.json"
---

<objective>
Marcadores, anotaÃ§Ãµes, hinÃ¡rios e planos de leitura.

Purpose: PER-01 (marcadores 5 cores), PER-02 (anotaÃ§Ãµes), PER-03 (hinÃ¡rios), EST-05 (planos com progresso). Tudo persistido em IndexedDB local (stores study e plans) â€” sem backend nesta fase (D-06/D-07; sync Supabase fica para a Fase 3). O verse-actions Ã© o bloco central: um bottom sheet por versÃ­culo reutilizÃ¡vel que o 03-01/03-02 (Ã¡udio/sync) estenderÃ£o.
Output: versÃ­culos marcÃ¡veis/anotÃ¡veis com persistÃªncia, hinÃ¡rio offline, planos de leitura com progresso persistido.
</objective>

<execution_context>
@~/.config/opencode/gsd-core/workflows/execute-plan.md
@~/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-estudo-e-personaliza-o/02-CONTEXT.md
@.planning/phases/02-estudo-e-personaliza-o/02-RESEARCH.md
@.planning/phases/02-estudo-e-personaliza-o/02-02-SUMMARY.md
@src/lib/bible.ts
@src/components/reader.tsx
@src/components/book-picker.tsx
@src/components/study-view.tsx
@src/lib/study.ts
@scripts/study-build.mjs
@scripts/generate-data.mjs
</context>

<tasks>

<task type="tracer" reversibility="costly">
  <name>Task 1: VersÃ­culo clicÃ¡vel â†’ marcador 5 cores + anotaÃ§Ã£o â†’ persistidos no store study â€” PER-01, PER-02, D-06, D-07</name>
  <files>src/lib/bible.ts, src/components/verse-actions.tsx, src/components/reader.tsx</files>
  <read_first>
    - src/lib/bible.ts (openDB v2 jÃ¡ criado no 02-01 com os stores "study" e "plans" â€” USAR os existentes, nÃ£o re-criar; estender accessors: putStudyRecord/getStudyRecords/getAllStudyRecords/deleteStudyRecord/getPlanProgress/setPlanProgress)
    - src/components/reader.tsx (ChapterView â€” loop de versÃ­culos onde plugar cliques e destaques)
    - src/components/book-picker.tsx (padrÃ£o de bottom sheet do projeto â€” copiar estrutura/estilo)
    - 02-CONTEXT.md D-06 (5 cores fixas, sync-ready, storage local) e D-07 (anotaÃ§Ãµes texto puro)
    - 02-02-SUMMARY.md (se existir â€” versÃ£o atual do bible.ts/reader)
  </read_first>
  <acceptance_criteria>
    - E2E: abrir Gn 1 â†’ clicar versÃ­culo 1 â†’ sheet abre com 5 cores â†’ tocar amarelo â†’ o versÃ­culo ganha backgroundColor rgb(254, 240, 138) via style inline (Pitfall 3)
    - E2E: reload da pÃ¡gina â†’ destaque persiste (leitura de getStudyRecords no efeito do capÃ­tulo)
    - E2E: abrir sheet de novo â†’ digitar anotaÃ§Ã£o â†’ Salvar â†’ marcador de nota visÃ­vel no versÃ­culo (ex.: underline/Ã­cone)
    - node -e via IDB (ou E2E assert): store study contÃ©m registro { id: "tb:0:0:0", color, text, updatedAt }
    - E2E: Remover â†’ destaque some e registro deletado
    - `npm run lint`/`npx tsc --noEmit`/`npm run build` passam; 0 erros console
  </acceptance_criteria>
  <action>
    src/lib/bible.ts: usar os stores "study" (keyPath "id") e "plans" (keyPath "planId") JÃ CRIADOS no 02-01 Task 3 (guard idempotente do onupgradeneeded â€” nÃ£o re-criar nem alterar o schema); export interface StudyRecord { id: string; ref: { version: string; book: number; chapter: number; verse: number }; color: string | null; text: string | null; updatedAt: number } â€” id no formato `${version}:${book}:${chapter}:${verse}` (D-06); export async function getStudyRecords(version, book, chapter): Promise<StudyRecord[]> via IDBKeyRange.bound(`${version}:${book}:${chapter}:`, `${version}:${book}:${chapter}:\uffff`) sobre o store study; export async function getAllStudyRecords(): Promise<StudyRecord[]> (store.getAll â€” usado na aba Notas da Task 2); export async function putStudyRecord(record: StudyRecord) (store.put upsert); export async function deleteStudyRecord(id) (store.delete).
    Criar src/components/verse-actions.tsx ("use client"): bottom sheet overlay no padrÃ£o do book-picker; props { open, verse: { book, chapter, verse } | null, version, label: string, initial: StudyRecord | null, onClose, onChanged }. Export const HIGHLIGHT_COLORS: Record<string, string> = { amarelo: "#fef08a", verde: "#bbf7d0", azul: "#bfdbfe", rosa: "#fbcfe8", laranja: "#fed7aa" } (5 cores fixas â€” D-06). Toque numa cor â†’ putStudyRecord({ id, ref, color: hex, text: initial?.text ?? null, updatedAt: Date.now() }) â†’ onChanged(). Textarea (maxLength 2000, aria-label "AnotaÃ§Ã£o") + botÃ£o "Salvar anotaÃ§Ã£o" â†’ putStudyRecord({ ...color: initial?.color ?? null, text }) â†’ onChanged(). BotÃ£o "Remover" (sÃ³ se initial) â†’ deleteStudyRecord(id) â†’ onChanged(). onClose fecha. Sem dependÃªncias novas.
    src/components/reader.tsx: no loop de versÃ­culos do ChapterView, cada versÃ­culo vira <button type="button" aria-label={`VersÃ­culo ${i+1} (${abbrev} ${chapter}:${i+1})`}> com classe base existente e style inline backgroundColor: record?.color quando houver (Pitfall 3 â€” NUNCA classes dinÃ¢micas); marcador de anotaÃ§Ã£o (underline pontilhado ou Ã­cone pequeno) quando record?.text; onClick â†’ abrir VerseActions com initial; carregar const records = await getStudyRecords(version, pos.bookId, pos.chapter) no efeito de capÃ­tulo; apÃ³s onChanged re-carregar os records do capÃ­tulo.
  </action>
  <verify>
    <automated>npm run lint; npx tsc --noEmit; npm run build; E2E (playwright, padrÃ£o Fase 1): fluxo completo marcador/nota/remover + reload com persistÃªncia + 0 erros console; assertion de store IDB via page.evaluate (getAll do store study e checar id tb:0:0:0 com color #fef08a)</automated>
  </verify>
  <done>VersÃ­culos clicÃ¡veis com 5 cores fixas e anotaÃ§Ãµes, persistidos em IndexedDB (store study) e refletidos no leitor; painel reutilizÃ¡vel verse-actions pronto para extensÃ£o na Fase 3.</done>
</task>

<task type="auto" reversibility="reversible">
  <name>Task 2: HinÃ¡rios + aba Notas â€” PER-03, D-09, PER-02</name>
  <files>data/raw/hymns.json, scripts/study-build.mjs, src/lib/study.ts, src/components/study-view.tsx</files>
  <read_first>
    - 02-CONTEXT.md D-09 (hinÃ¡rios domÃ­nio pÃºblico, mitigaÃ§Ã£o A1 â‰¥30)
    - scripts/study-build.mjs (da Task 2 do 02-02 â€” estender)
    - src/lib/study.ts (getDictionary/getThemes â€” padrÃ£o dos getters)
    - src/components/study-view.tsx (abas atuais â€” adicionar Hinos e Notas)
  </read_first>
  <acceptance_criteria>
    - node -e exec: public/data/study/hymns.json Ã© array com â‰¥30 objetos {id: string, title: string, verses: string[]} â€” verses nÃ£o vazio e cada verso string nÃ£o vazia
    - E2E: aba Estudo â†’ Hinos â†’ lista â†’ abrir hino â†’ letra completa renderizada
    - E2E: aba Notas â†’ lista com as anotaÃ§Ãµes criadas na Task 1 (abbrev cap:versÃ­culo + texto) â†’ clique navega para o capÃ­tulo
    - `npm run lint`/`npx tsc --noEmit`/`npm run build` passam; 0 erros console
  </acceptance_criteria>
  <action>
    data/raw/hymns.json: curar ~50 hinos de domÃ­nio pÃºblico {id, title, verses: string[]} â€” clÃ¡ssicos PD confirmados (Sublime GraÃ§a, Castelo Forte, Grandioso Ã‰s Tu, Noite Feliz, Firmes nas Promessas, Oh! Que Amigo Temos em Jesus, Mais Perto Quero Estar, QuÃ£o Grande Ã‰s Tu, A BÃ­blia, Santa Ceia...). MitigaÃ§Ã£o A1: mÃ­nimo 30 aceito. Para cada hino, breadcrumb canÃ´nico (canon-referral, sem mint): versÃ£o do hino deve ser de fonte PD verificÃ¡vel (hino clÃ¡ssico prÃ©-1927 ou traduÃ§Ã£o PD); se houver dÃºvida de copyright de uma traduÃ§Ã£o especÃ­fica, registrar no SUMMARY e trocar por outro clÃ¡ssico PD â€” nunca assumir PD (breadcrumb).
    scripts/study-build.mjs: adicionar leitura/validaÃ§Ã£o de data/raw/hymns.json (id/title strings, verses array de strings nÃ£o vazias) e escrita de public/data/study/hymns.json; graceful skip se raw ausente; logar contagem.
    src/lib/study.ts: export interface Hymn { id: string; title: string; verses: string[] }; export async function getHymns(): Promise<Hymn[]> (fetch cache, padrÃ£o getThemes).
    src/components/study-view.tsx: adicionar abas "Hinos" e "Notas" ao lado das atuais. Hinos: lista (title) â†’ expandir (useState) â†’ letra com versos em parÃ¡grafos. Notas: carregar getAllStudyRecords() (da Task 1) â†’ filtrar text != null â†’ renderizar abbrev (index.books[book].abbrev) + cap:versÃ­culo + text + cor da marcaÃ§Ã£o (se houver) â†’ clique â†’ onNavigate(book, chapter); estado vazio "Nenhuma anotaÃ§Ã£o".
  </action>
  <verify>
    <automated>node scripts/study-build.mjs; node -e "const h=require('./public/data/study/hymns.json'); if(!Array.isArray(h)||h.length<30||h.some(x=>typeof x.id!=='string'||typeof x.title!=='string'||!Array.isArray(x.verses)||x.verses.length===0||x.verses.some(v=>typeof v!=='string'||!v.trim())))process.exit(1)"; npm run lint; npx tsc --noEmit; npm run build; E2E: Hinos (expandir letra) e Notas (lista + navegaÃ§Ã£o) com 0 erros console</automated>
  </verify>
  <done>HinÃ¡rio â‰¥30 hinos PD precached e legÃ­vel na aba Estudo; aba Notas lista anotaÃ§Ãµes existentes e navega para o capÃ­tulo.</done>
</task>

<task type="auto" reversibility="reversible">
  <name>Task 3: Planos de leitura com progresso persistido â€” EST-05, D-10</name>
  <files>scripts/study-build.mjs, src/lib/bible.ts, src/lib/study.ts, src/components/study-view.tsx</files>
  <read_first>
    - 02-CONTEXT.md D-10 (2 planos fixos, progresso em IDB)
    - scripts/study-build.mjs (da Task 2 do 02-02 e Task 2 deste plano â€” estender)
    - src/lib/bible.ts (accessors IDB â€” adicionar PlanProgress CRUD)
    - src/components/study-view.tsx (abas atuais â€” adicionar Planos)
  </read_first>
  <acceptance_criteria>
    - node -e exec: public/data/study/plans.json tem 2 planos {id, title, description, totalDays, days: [{day, readings: [{book, chapter}]}]} com totalDays 365 e 90; todos os book (0-65) e chapter (< book.chapters) vÃ¡lidos contra public/data/index.json
    - E2E: aba Estudo â†’ Planos â†’ card "BÃ­blia em 1 ano" mostra progresso 0/365 (0%) â†’ expandir â†’ dia 1 com leituras (Gn 1, Gn 2, Gn 3) â†’ botÃ£o "Marcar dia 1 concluÃ­do" â†’ progresso 1/365 â†’ reload â†’ persiste (store plans no IDB)
    - E2E: clique numa leitura do dia â†’ leitor abre ?b=&c=
    - E2E: abrir/ler NÃƒO marca dia automaticamente (progresso sÃ³ muda por aÃ§Ã£o explÃ­cita)
    - `npm run lint`/`npx tsc --noEmit`/`npm run build` passam; 0 erros console
  </acceptance_criteria>
  <action>
    scripts/study-build.mjs: gerar public/data/study/plans.json ALGORITMICAMENTE do public/data/index.json (sem duplicar contagem de capÃ­tulos): plano "bib1ano" { id, title: "BÃ­blia em 1 ano", description, totalDays: 365 } â€” percorrer os 66 livros em ordem (AT+NT) distribuindo ~3 capÃ­tulos/dia sequencial; plano "nt90" { id, title: "Novo Testamento em 90 dias", totalDays: 90 } â€” livros 39-65 (NT: Mateus=39 .. Apocalipse=65, convenÃ§Ã£o 0-indexada do plano â€” tb:0:0:0 = GÃªnesis; acceptance book (0-65)), ~1-2 capÃ­tulos/dia. Dias com readings: [{ book: bookId, chapter: n }]. Escrever e logar "plans: 2 planos, N dias".
    src/lib/bible.ts: export interface PlanProgress { planId: string; completedDays: number[]; updatedAt: number }; export async function getPlanProgress(planId): Promise<PlanProgress | null> (store plans, keyPath planId); export async function setPlanProgress(planId, completedDays: number[]): Promise<void> (put com updatedAt: Date.now()).
    src/lib/study.ts: export types DailyReading { book: number; chapter: number }, PlanDay { day: number; readings: DailyReading[] }, Plan { id: string; title: string; description: string; totalDays: number; days: PlanDay[] }; export async function getPlans(): Promise<Plan[]> (fetch cache).
    src/components/study-view.tsx: adicionar aba "Planos". Card por plano (title + description + barra "X/totalDays (Y%)" via getPlanProgress); expandir â†’ lista de dias (leitura como `index.books[book].abbrev  chapter`) com clique â†’ onNavigate(book, chapter); botÃ£o por dia "Marcar dia N concluÃ­do" (aÃ§Ã£o explÃ­cita â€” nunca marcar ao abrir) â†’ setPlanProgress(planId, [...completedDays, day]) â†’ re-render; toggle desmarca se jÃ¡ concluÃ­do. Carregar getPlans() no mount.
  </action>
  <verify>
    <automated>node scripts/study-build.mjs; node -e "const p=require('./public/data/study/plans.json'); if(!Array.isArray(p)||p.length!==2||p[0].totalDays!==365||p[1].totalDays!==90)process.exit(1); const idx=require('./public/data/index.json'); for(const plan of p){for(const d of plan.days){for(const r of d.readings){const b=idx.books[r.book]; if(!b||r.chapter<1||r.chapter>b.chapters)process.exit(1)}}}"; npm run lint; npx tsc --noEmit; npm run build; E2E: progresso 0â†’1â†’persiste apÃ³s reload + navegaÃ§Ã£o da leitura + verificaÃ§Ã£o de que abrir leitura nÃ£o marca dia (padrÃ£o Fase 1)</automated>
  </verify>
  <done>2 planos de leitura precached com progresso persistido em IDB (store plans), marcÃ¡vel sÃ³ por aÃ§Ã£o explÃ­cita, com navegaÃ§Ã£o para as leituras.</done>
</task>

</tasks>

## Flagged Assumptions (edge probe â€” specless fallback)

| Requirement | Edge | Resolution |
|-------------|------|------------|
| PER-01 | Formato de persistÃªncia sem backend definido | Resolvido explÃ­cito â€” D-06 (IDB local, id version:book:chapter:verse, updatedAt sync-ready, Fase 3 sync) |
| PER-02 | Onde o usuÃ¡rio vÃª as anotaÃ§Ãµes depois | Resolvido explÃ­cito â€” aba Notas na StudyView (Task 2) + marcador no versÃ­culo |
| PER-03 | ConteÃºdo do hinÃ¡rio nÃ£o existe antes da fase | Resolvido explÃ­cito (Task 2) + backstop (verification: backstop): mÃ­n. 30 hinos na mitigaÃ§Ã£o A1; breadcrumb de PD verificado por hino |
| EST-05 | Como marcar dias do plano | Resolvido explÃ­cito â€” aÃ§Ã£o explÃ­cita por dia; nunca auto-marca ao abrir (prohibition) |

Breadcrumb canÃ´nico (canon-referral, sem mint): copyright dos hinos â€” apenas clÃ¡ssicos PD prÃ©-1927 ou traduÃ§Ãµes PD verificÃ¡veis; dÃºvida â†’ trocar hino e registrar no SUMMARY (Task 2). XSS via anotaÃ§Ãµes â€” texto renderizado via React (escaped); sem dangerouslySetInnerHTML.

## Prohibitions

- Nunca marcar progresso de plano de leitura automaticamente â€” progresso sÃ³ muda por aÃ§Ã£o explÃ­cita do usuÃ¡rio (EST-05, D-10; botÃ£o "Marcar dia N concluÃ­do" na Task 3). Status: flagged â€” E2E deve provar que abrir/ler leitura nÃ£o marca dia.
- Nunca usar dangerouslySetInnerHTML â€” todo conteÃºdo (corpus, hinos, dicionÃ¡rio, anotaÃ§Ãµes, snippets de busca) Ã© renderizado como texto puro via React (XSS canÃ´nico, V5 ASVS). Status: flagged â€” grep no verify (`grep -rn "dangerouslySetInnerHTML" src/` deve retornar 0).

## Artifacts this phase produces

- `src/lib/bible.ts` â€” `StudyRecord` CRUD + `PlanProgress` CRUD (stores study/plans no schema v2)
- `src/components/verse-actions.tsx` â€” `HIGHLIGHT_COLORS` (5 cores fixas) + bottom sheet marcador/anotaÃ§Ã£o/remover
- `src/components/reader.tsx` â€” versÃ­culos clicÃ¡veis com destaque inline (Pitfall 3)
- `data/raw/hymns.json` (~50 PD) â†’ `public/data/study/hymns.json` (â‰¥30 aceito)
- `scripts/study-build.mjs` â€” + hymns.json + geraÃ§Ã£o algorÃ­tmica de plans.json do index.json
- `src/lib/study.ts` â€” `Hymn`, `Plan`, `DailyReading`, `getHymns()`, `getPlans()`
- `src/components/study-view.tsx` â€” abas Hinos, Notas, Planos

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| clientâ†’IDB store study | AnotaÃ§Ãµes/marcaÃ§Ãµes escritas pelo usuÃ¡rio localmente (sincronizar na Fase 3) |
| clientâ†’/data/study/hymns.json + plans.json | EstÃ¡ticos first-party gerados no build |
| clientâ†’IDB store plans | Progresso de leitura local |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-03-01 | Tampering | AnotaÃ§Ã£o do usuÃ¡rio renderizada | medium | mitigate | Render via React (escaping); sem dangerouslySetInnerHTML; maxLength 2000 no textarea |
| T-02-03-02 | Tampering | Dados locais study/plans | low | accept | Dados locais first-party do prÃ³prio usuÃ¡rio; perda = aceitÃ¡vel nesta fase (sync Fase 3 via updatedAt) |
| T-02-03-03 | Tampering | ConteÃºdo hinÃ¡rio | medium | mitigate | Curadoria PD verificada (breadcrumb); shape validado no study-build.mjs |
| T-02-03-SC | Tampering | npm (nenhum pacote novo neste plano) | high | mitigate | Nenhum install novo; minisearch/serwist jÃ¡ auditados |
</threat_model>

<verification>
- `npm run lint`, `npx tsc --noEmit`, `npm run build` â€” PASS apÃ³s cada task
- AsserÃ§Ãµes node: hymns â‰¥30 shape vÃ¡lido; plans.json 2 planos com readings vÃ¡lidos contra index.json
- E2E: marcaÃ§Ã£o/anotaÃ§Ã£o persistÃªncia + reload; hinos expandem; notas navegam; plano marca/desmarca/persiste; 0 erros console
</verification>

<success_criteria>
- PER-01: marcador 5 cores persistido â€” VERIFICADO por E2E (inline style + reload)
- PER-02: anotaÃ§Ã£o criada e visÃ­vel (painel + aba Notas) â€” VERIFICADO por E2E
- PER-03: hinÃ¡rio â‰¥30 PD offline â€” VERIFICADO por node assert + E2E
- EST-05: 2 planos com progresso persistido â€” VERIFICADO por node assert + E2E
</success_criteria>

<output>
Create `.planning/phases/02-estudo-e-personaliza-o/02-03-SUMMARY.md` when done
</output>

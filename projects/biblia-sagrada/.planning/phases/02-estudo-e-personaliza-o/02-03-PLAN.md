---
phase: 02-estudo-e-personaliza-o
plan: 03
type: execute
wave: 3
depends_on: [02-01, 02-02]
files_modified:
  - src/lib/bible.ts
  - src/components/verse-actions.tsx
  - src/components/reader.tsx
  - data/raw/hymns.json
  - scripts/study-build.mjs
  - src/lib/study.ts
  - src/components/study-view.tsx
autonomous: true
requirements: [PER-01, PER-02, PER-03, EST-05]
estimate:
  tokens: 65000
  raw_tokens: 65000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Usuário marca versículos com 5 cores fixas, aplicadas inline e persistidas no store study (sync-ready Fase 3) [PER-01, D-06, D-07]"
    - "Usuário cria e vê anotações por versículo (painel de ação + lista na aba Estudo) [PER-02]"
    - "Usuário acessa hinários com letra completa (~50 hinos, mínimo aceito 30, todos domínio público verificado) [PER-03, D-09, A1]"
    - "Usuário acompanha 2 planos de leitura fixos (Bíblia em 1 ano, NT em 90 dias) com progresso persistido no IDB [EST-05, D-10]"
    - "Progresso de plano de leitura nunca é marcado automaticamente — só por ação explícita do usuário [EST-05]"
    - statement: "Conteúdo de hinário (≥30 hinos PD) e planos (2 gerados do index.json) são produzidos nesta fase e versionados pelo pipeline [A1/OQ-1]"
      verification: backstop
  artifacts:
    - path: "src/components/verse-actions.tsx"
      provides: "Bottom sheet de ação por versículo: 5 cores + anotação + remover (padrão book-picker)"
      min_lines: 40
    - path: "src/lib/bible.ts"
      provides: "StudyRecord CRUD (putStudyRecord/getStudyRecords/getAllStudyRecords/deleteStudyRecord) + PlanProgress (getPlanProgress/setPlanProgress)"
      contains: "putStudyRecord"
    - path: "src/components/reader.tsx"
      provides: "Versículos clicáveis com destaque inline (cor/nota) e abertura do verse-actions"
      contains: "getStudyRecords"
    - path: "data/raw/hymns.json"
      provides: "~50 hinos {id, title, verses: string[]} — domínio público"
      min_lines: 20
    - path: "scripts/study-build.mjs"
      provides: "Estende com hymns.json + gera plans.json algoritmicamente do index.json"
      contains: "plans"
    - path: "src/lib/study.ts"
      provides: "getHymns()/getPlans() + tipos Hymn/Plan/DailyReading"
      contains: "getHymns"
    - path: "src/components/study-view.tsx"
      provides: "Abas Hinos, Notas e Planos (expansão do 02-02)"
      contains: "Notas"
    - path: "public/data/study/hymns.json"
      provides: "Hinário precached"
      min_lines: 1
    - path: "public/data/study/plans.json"
      provides: "2 planos de leitura precached"
      min_lines: 1
  key_links:
    - from: "src/components/reader.tsx"
      to: "src/lib/bible.ts"
      via: "getStudyRecords(version, book, chapter) no efeito de capítulo + reload após onChanged (Pitfall 3 — cores via style inline, nunca classes dinâmicas)"
      pattern: "getStudyRecords"
    - from: "src/components/verse-actions.tsx"
      to: "src/lib/bible.ts"
      via: "putStudyRecord/deleteStudyRecord — id `${version}:${book}:${chapter}:${verse}` (D-06), updatedAt em cada escrita (Fase 3 sync)"
      pattern: "putStudyRecord"
    - from: "src/components/study-view.tsx"
      to: "URL"
      via: "onNavigate(book, chapter) → ?b=&c= via replaceState string (Pitfall 4)"
      pattern: "replaceState"
    - from: "scripts/study-build.mjs"
      to: "public/data/index.json"
      via: "gera plans.json do índice (66 livros com chapters) — sem duplicar contagem"
      pattern: "index.json"
---

<objective>
Marcadores, anotações, hinários e planos de leitura.

Purpose: PER-01 (marcadores 5 cores), PER-02 (anotações), PER-03 (hinários), EST-05 (planos com progresso). Tudo persistido em IndexedDB local (stores study e plans) — sem backend nesta fase (D-06/D-07; sync Supabase fica para a Fase 3). O verse-actions é o bloco central: um bottom sheet por versículo reutilizável que o 03-01/03-02 (áudio/sync) estenderão.
Output: versículos marcáveis/anotáveis com persistência, hinário offline, planos de leitura com progresso persistido.
</objective>

<execution_context>
@C:/Users/VCM37/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/VCM37/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-estudo-e-personaliza-o/02-CONTEXT.md
@.planning/phases/02-estudo-e-personaliza-o/02-RESEARCH.md
@.planning/phases/02-estudo-e-personaliza-o/02-02-SUMMARY.md
@src/lib/bible.ts
@src/components/reader.tsx
@src/components/book-picker.tsx
@src/components/study-view.tsx
@src/lib/study.ts
@scripts/study-build.mjs
@scripts/generate-data.mjs
</context>

<tasks>

<task type="tracer" reversibility="costly">
  <name>Task 1: Versículo clicável → marcador 5 cores + anotação → persistidos no store study — PER-01, PER-02, D-06, D-07</name>
  <files>src/lib/bible.ts, src/components/verse-actions.tsx, src/components/reader.tsx</files>
  <read_first>
    - src/lib/bible.ts (openDB v2 já criado no 02-01 — adicionar stores study e plans aqui; padrão dos accessors)
    - src/components/reader.tsx (ChapterView — loop de versículos onde plugar cliques e destaques)
    - src/components/book-picker.tsx (padrão de bottom sheet do projeto — copiar estrutura/estilo)
    - 02-CONTEXT.md D-06 (5 cores fixas, sync-ready, storage local) e D-07 (anotações texto puro)
    - 02-02-SUMMARY.md (se existir — versão atual do bible.ts/reader)
  </read_first>
  <acceptance_criteria>
    - E2E: abrir Gn 1 → clicar versículo 1 → sheet abre com 5 cores → tocar amarelo → o versículo ganha backgroundColor rgb(254, 240, 138) via style inline (Pitfall 3)
    - E2E: reload da página → destaque persiste (leitura de getStudyRecords no efeito do capítulo)
    - E2E: abrir sheet de novo → digitar anotação → Salvar → marcador de nota visível no versículo (ex.: underline/ícone)
    - node -e via IDB (ou E2E assert): store study contém registro { id: "tb:0:0:0", color, text, updatedAt }
    - E2E: Remover → destaque some e registro deletado
    - `npm run lint`/`npx tsc --noEmit`/`npm run build` passam; 0 erros console
  </acceptance_criteria>
  <action>
    src/lib/bible.ts: adicionar ao schema v2 os stores "study" (keyPath "id") e "plans" (keyPath "planId"); export interface StudyRecord { id: string; ref: { version: string; book: number; chapter: number; verse: number }; color: string | null; text: string | null; updatedAt: number } — id no formato `${version}:${book}:${chapter}:${verse}` (D-06); export async function getStudyRecords(version, book, chapter): Promise<StudyRecord[]> via IDBKeyRange.bound(`${version}:${book}:${chapter}:`, `${version}:${book}:${chapter}:\uffff`) sobre o store study; export async function getAllStudyRecords(): Promise<StudyRecord[]> (store.getAll — usado na aba Notas da Task 2); export async function putStudyRecord(record: StudyRecord) (store.put upsert); export async function deleteStudyRecord(id) (store.delete).
    Criar src/components/verse-actions.tsx ("use client"): bottom sheet overlay no padrão do book-picker; props { open, verse: { book, chapter, verse } | null, version, label: string, initial: StudyRecord | null, onClose, onChanged }. Export const HIGHLIGHT_COLORS: Record<string, string> = { amarelo: "#fef08a", verde: "#bbf7d0", azul: "#bfdbfe", rosa: "#fbcfe8", laranja: "#fed7aa" } (5 cores fixas — D-06). Toque numa cor → putStudyRecord({ id, ref, color: hex, text: initial?.text ?? null, updatedAt: Date.now() }) → onChanged(). Textarea (maxLength 2000, aria-label "Anotação") + botão "Salvar anotação" → putStudyRecord({ ...color: initial?.color ?? null, text }) → onChanged(). Botão "Remover" (só se initial) → deleteStudyRecord(id) → onChanged(). onClose fecha. Sem dependências novas.
    src/components/reader.tsx: no loop de versículos do ChapterView, cada versículo vira <button type="button" aria-label={`Versículo ${i+1} (${abbrev} ${chapter}:${i+1})`}> com classe base existente e style inline backgroundColor: record?.color quando houver (Pitfall 3 — NUNCA classes dinâmicas); marcador de anotação (underline pontilhado ou ícone pequeno) quando record?.text; onClick → abrir VerseActions com initial; carregar const records = await getStudyRecords(version, pos.bookId, pos.chapter) no efeito de capítulo; após onChanged re-carregar os records do capítulo.
  </action>
  <verify>
    <automated>npm run lint; npx tsc --noEmit; npm run build; E2E (playwright, padrão Fase 1): fluxo completo marcador/nota/remover + reload com persistência + 0 erros console; assertion de store IDB via page.evaluate (getAll do store study e checar id tb:0:0:0 com color #fef08a)</automated>
  </verify>
  <done>Versículos clicáveis com 5 cores fixas e anotações, persistidos em IndexedDB (store study) e refletidos no leitor; painel reutilizável verse-actions pronto para extensão na Fase 3.</done>
</task>

<task type="auto" reversibility="reversible">
  <name>Task 2: Hinários + aba Notas — PER-03, D-09, PER-02</name>
  <files>data/raw/hymns.json, scripts/study-build.mjs, src/lib/study.ts, src/components/study-view.tsx</files>
  <read_first>
    - 02-CONTEXT.md D-09 (hinários domínio público, mitigação A1 ≥30)
    - scripts/study-build.mjs (da Task 2 do 02-02 — estender)
    - src/lib/study.ts (getDictionary/getThemes — padrão dos getters)
    - src/components/study-view.tsx (abas atuais — adicionar Hinos e Notas)
  </read_first>
  <acceptance_criteria>
    - node -e exec: public/data/study/hymns.json é array com ≥30 objetos {id: string, title: string, verses: string[]} — verses não vazio e cada verso string não vazia
    - E2E: aba Estudo → Hinos → lista → abrir hino → letra completa renderizada
    - E2E: aba Notas → lista com as anotações criadas na Task 1 (abbrev cap:versículo + texto) → clique navega para o capítulo
    - `npm run lint`/`npx tsc --noEmit`/`npm run build` passam; 0 erros console
  </acceptance_criteria>
  <action>
    data/raw/hymns.json: curar ~50 hinos de domínio público {id, title, verses: string[]} — clássicos PD confirmados (Sublime Graça, Castelo Forte, Grandioso És Tu, Noite Feliz, Firmes nas Promessas, Oh! Que Amigo Temos em Jesus, Mais Perto Quero Estar, Quão Grande És Tu, A Bíblia, Santa Ceia...). Mitigação A1: mínimo 30 aceito. Para cada hino, breadcrumb canônico (canon-referral, sem mint): versão do hino deve ser de fonte PD verificável (hino clássico pré-1927 ou tradução PD); se houver dúvida de copyright de uma tradução específica, registrar no SUMMARY e trocar por outro clássico PD — nunca assumir PD (breadcrumb).
    scripts/study-build.mjs: adicionar leitura/validação de data/raw/hymns.json (id/title strings, verses array de strings não vazias) e escrita de public/data/study/hymns.json; graceful skip se raw ausente; logar contagem.
    src/lib/study.ts: export interface Hymn { id: string; title: string; verses: string[] }; export async function getHymns(): Promise<Hymn[]> (fetch cache, padrão getThemes).
    src/components/study-view.tsx: adicionar abas "Hinos" e "Notas" ao lado das atuais. Hinos: lista (title) → expandir (useState) → letra com versos em parágrafos. Notas: carregar getAllStudyRecords() (da Task 1) → filtrar text != null → renderizar abbrev (index.books[book].abbrev) + cap:versículo + text + cor da marcação (se houver) → clique → onNavigate(book, chapter); estado vazio "Nenhuma anotação".
  </action>
  <verify>
    <automated>node scripts/study-build.mjs; node -e "const h=require('./public/data/study/hymns.json'); if(!Array.isArray(h)||h.length<30||h.some(x=>typeof x.id!=='string'||typeof x.title!=='string'||!Array.isArray(x.verses)||x.verses.length===0||x.verses.some(v=>typeof v!=='string'||!v.trim())))process.exit(1)"; npm run lint; npx tsc --noEmit; npm run build; E2E: Hinos (expandir letra) e Notas (lista + navegação) com 0 erros console</automated>
  </verify>
  <done>Hinário ≥30 hinos PD precached e legível na aba Estudo; aba Notas lista anotações existentes e navega para o capítulo.</done>
</task>

<task type="auto" reversibility="reversible">
  <name>Task 3: Planos de leitura com progresso persistido — EST-05, D-10</name>
  <files>scripts/study-build.mjs, src/lib/bible.ts, src/lib/study.ts, src/components/study-view.tsx</files>
  <read_first>
    - 02-CONTEXT.md D-10 (2 planos fixos, progresso em IDB)
    - scripts/study-build.mjs (da Task 2 do 02-02 e Task 2 deste plano — estender)
    - src/lib/bible.ts (accessors IDB — adicionar PlanProgress CRUD)
    - src/components/study-view.tsx (abas atuais — adicionar Planos)
  </read_first>
  <acceptance_criteria>
    - node -e exec: public/data/study/plans.json tem 2 planos {id, title, description, totalDays, days: [{day, readings: [{book, chapter}]}]} com totalDays 365 e 90; todos os book (0-65) e chapter (< book.chapters) válidos contra public/data/index.json
    - E2E: aba Estudo → Planos → card "Bíblia em 1 ano" mostra progresso 0/365 (0%) → expandir → dia 1 com leituras (Gn 1, Gn 2, Gn 3) → botão "Marcar dia 1 concluído" → progresso 1/365 → reload → persiste (store plans no IDB)
    - E2E: clique numa leitura do dia → leitor abre ?b=&c=
    - E2E: abrir/ler NÃO marca dia automaticamente (progresso só muda por ação explícita)
    - `npm run lint`/`npx tsc --noEmit`/`npm run build` passam; 0 erros console
  </acceptance_criteria>
  <action>
    scripts/study-build.mjs: gerar public/data/study/plans.json ALGORITMICAMENTE do public/data/index.json (sem duplicar contagem de capítulos): plano "bib1ano" { id, title: "Bíblia em 1 ano", description, totalDays: 365 } — percorrer os 66 livros em ordem (AT+NT) distribuindo ~3 capítulos/dia sequencial; plano "nt90" { id, title: "Novo Testamento em 90 dias", totalDays: 90 } — livros 40-66 (NT), ~1-2 capítulos/dia. Dias com readings: [{ book: bookId, chapter: n }]. Escrever e logar "plans: 2 planos, N dias".
    src/lib/bible.ts: export interface PlanProgress { planId: string; completedDays: number[]; updatedAt: number }; export async function getPlanProgress(planId): Promise<PlanProgress | null> (store plans, keyPath planId); export async function setPlanProgress(planId, completedDays: number[]): Promise<void> (put com updatedAt: Date.now()).
    src/lib/study.ts: export types DailyReading { book: number; chapter: number }, PlanDay { day: number; readings: DailyReading[] }, Plan { id: string; title: string; description: string; totalDays: number; days: PlanDay[] }; export async function getPlans(): Promise<Plan[]> (fetch cache).
    src/components/study-view.tsx: adicionar aba "Planos". Card por plano (title + description + barra "X/totalDays (Y%)" via getPlanProgress); expandir → lista de dias (leitura como `index.books[book].abbrev  chapter`) com clique → onNavigate(book, chapter); botão por dia "Marcar dia N concluído" (ação explícita — nunca marcar ao abrir) → setPlanProgress(planId, [...completedDays, day]) → re-render; toggle desmarca se já concluído. Carregar getPlans() no mount.
  </action>
  <verify>
    <automated>node scripts/study-build.mjs; node -e "const p=require('./public/data/study/plans.json'); if(!Array.isArray(p)||p.length!==2||p[0].totalDays!==365||p[1].totalDays!==90)process.exit(1); const idx=require('./public/data/index.json'); for(const plan of p){for(const d of plan.days){for(const r of d.readings){const b=idx.books[r.book]; if(!b||r.chapter<1||r.chapter>b.chapters)process.exit(1)}}}"; npm run lint; npx tsc --noEmit; npm run build; E2E: progresso 0→1→persiste após reload + navegação da leitura + verificação de que abrir leitura não marca dia (padrão Fase 1)</automated>
  </verify>
  <done>2 planos de leitura precached com progresso persistido em IDB (store plans), marcável só por ação explícita, com navegação para as leituras.</done>
</task>

</tasks>

## Flagged Assumptions (edge probe — specless fallback)

| Requirement | Edge | Resolution |
|-------------|------|------------|
| PER-01 | Formato de persistência sem backend definido | Resolvido explícito — D-06 (IDB local, id version:book:chapter:verse, updatedAt sync-ready, Fase 3 sync) |
| PER-02 | Onde o usuário vê as anotações depois | Resolvido explícito — aba Notas na StudyView (Task 2) + marcador no versículo |
| PER-03 | Conteúdo do hinário não existe antes da fase | Resolvido explícito (Task 2) + backstop (verification: backstop): mín. 30 hinos na mitigação A1; breadcrumb de PD verificado por hino |
| EST-05 | Como marcar dias do plano | Resolvido explícito — ação explícita por dia; nunca auto-marca ao abrir (prohibition) |

Breadcrumb canônico (canon-referral, sem mint): copyright dos hinos — apenas clássicos PD pré-1927 ou traduções PD verificáveis; dúvida → trocar hino e registrar no SUMMARY (Task 2). XSS via anotações — texto renderizado via React (escaped); sem dangerouslySetInnerHTML.

## Artifacts this phase produces

- `src/lib/bible.ts` — `StudyRecord` CRUD + `PlanProgress` CRUD (stores study/plans no schema v2)
- `src/components/verse-actions.tsx` — `HIGHLIGHT_COLORS` (5 cores fixas) + bottom sheet marcador/anotação/remover
- `src/components/reader.tsx` — versículos clicáveis com destaque inline (Pitfall 3)
- `data/raw/hymns.json` (~50 PD) → `public/data/study/hymns.json` (≥30 aceito)
- `scripts/study-build.mjs` — + hymns.json + geração algorítmica de plans.json do index.json
- `src/lib/study.ts` — `Hymn`, `Plan`, `DailyReading`, `getHymns()`, `getPlans()`
- `src/components/study-view.tsx` — abas Hinos, Notas, Planos

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→IDB store study | Anotações/marcações escritas pelo usuário localmente (sincronizar na Fase 3) |
| client→/data/study/hymns.json + plans.json | Estáticos first-party gerados no build |
| client→IDB store plans | Progresso de leitura local |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-03-01 | Tampering | Anotação do usuário renderizada | medium | mitigate | Render via React (escaping); sem dangerouslySetInnerHTML; maxLength 2000 no textarea |
| T-02-03-02 | Tampering | Dados locais study/plans | low | accept | Dados locais first-party do próprio usuário; perda = aceitável nesta fase (sync Fase 3 via updatedAt) |
| T-02-03-03 | Tampering | Conteúdo hinário | medium | mitigate | Curadoria PD verificada (breadcrumb); shape validado no study-build.mjs |
| T-02-03-SC | Tampering | npm (nenhum pacote novo neste plano) | high | mitigate | Nenhum install novo; minisearch/serwist já auditados |
</threat_model>

<verification>
- `npm run lint`, `npx tsc --noEmit`, `npm run build` — PASS após cada task
- Asserções node: hymns ≥30 shape válido; plans.json 2 planos com readings válidos contra index.json
- E2E: marcação/anotação persistência + reload; hinos expandem; notas navegam; plano marca/desmarca/persiste; 0 erros console
</verification>

<success_criteria>
- PER-01: marcador 5 cores persistido — VERIFICADO por E2E (inline style + reload)
- PER-02: anotação criada e visível (painel + aba Notas) — VERIFICADO por E2E
- PER-03: hinário ≥30 PD offline — VERIFICADO por node assert + E2E
- EST-05: 2 planos com progresso persistido — VERIFICADO por node assert + E2E
</success_criteria>

<output>
Create `.planning/phases/02-estudo-e-personaliza-o/02-03-SUMMARY.md` when done
</output>

---
phase: 02-estudo-e-personaliza-o
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/generate-data.mjs
  - next.config.ts
  - src/lib/settings.ts
  - src/lib/bible.ts
  - src/components/reader.tsx
  - src/components/version-picker.tsx
  - src/components/download-modal.tsx
autonomous: true
requirements: [LEI-02, OFF-02]
estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Usuário troca de tradução (tb ↔ alm1911) no mesmo capítulo sem recarregar visivelmente — texto do capítulo muda, URL ganha ?v=<code> e a escolha persiste entre sessões em localStorage (bs-version) [LEI-02, D-01, D-02, SC1]"
    - "BLIVRE aparece no seletor de tradução, é baixável sob demanda (66 livros → IndexedDB chapters) e legível offline após o download [OFF-02, D-03, SC5]"
    - "BLIVRE (public/data/blivre/**) e search/blivre não entram no precache do Service Worker — precache continua ~45MB [D-03, Pitfall 2]"
    - statement: "Após novo build (dataVersion muda), a UI não mente sobre BLIVRE baixada — ensureDataVersion limpa downloadedVersions + store search junto com chapters [Pitfall 5]"
      verification: backstop
  artifacts:
    - path: "src/lib/settings.ts"
      provides: "readVersion/writeVersion (localStorage bs-version)"
      contains: "bs-version"
    - path: "src/lib/bible.ts"
      provides: "DB_VERSION=2 + stores study/plans/search + meta downloadedVersions + downloadTranslation + getDownloadedVersions + putSearchIndex/getSearchIndex"
      contains: "DB_VERSION = 2"
    - path: "src/components/version-picker.tsx"
      provides: "Seletor de tradução no header (menu bottom-sheet, padrão book-picker)"
      min_lines: 40
    - path: "src/components/download-modal.tsx"
      provides: "Modal de progresso do download BLIVRE (done/66)"
      min_lines: 40
    - path: "src/components/reader.tsx"
      provides: "Versão dinâmica via estado React (removido const VERSION = \"tb\") + URL ?v= + localStorage"
      contains: "useState"
    - path: "scripts/generate-data.mjs"
      provides: "BLIVRE em public/data/blivre/** + index.json com 3 versões"
      contains: "blivre"
    - path: "next.config.ts"
      provides: "Filtro no walk de collectPublicFiles (exclui blivre + search/blivre)"
      contains: "startsWith(\"blivre\")"
    - path: "public/data/blivre/**"
      provides: "66 arquivos de livro BLIVRE embarcados, NÃO precacheados"
      min_lines: 1
  key_links:
    - from: "src/components/reader.tsx"
      to: "src/lib/bible.ts"
      via: "getChapter(version, bookId, chapter) — versão dinâmica em vez de VERSION fixo"
      pattern: "getChapter\\(version"
    - from: "src/components/reader.tsx"
      to: "URL"
      via: "history.replaceState(null, \"\", url.toString()) com ?v= (nunca objeto URL — Pitfall 4)"
      pattern: "replaceState\\(null, \"\", url\\.toString\\(\\)\\)"
    - from: "src/components/version-picker.tsx"
      to: "src/lib/bible.ts"
      via: "getDownloadedVersions() para badge de status e disparo do download"
      pattern: "getDownloadedVersions"
    - from: "next.config.ts"
      to: "public/sw.js"
      via: "walk() de collectPublicFiles ignora blivre → precache sem BLIVRE"
      pattern: "startsWith\\(\"blivre\"\\)"
    - from: "src/lib/bible.ts"
      to: "src/lib/bible.ts"
      via: "downloadTranslation → loadBookIntoStore (mesmo primitivo da Fase 1) por livro, idempotente"
      pattern: "loadBookIntoStore"
---

<objective>
Troca de traduções instantânea no leitor + download sob demanda da Bíblia Livre (BLIVRE) para IndexedDB, com leitura 100% offline.

Purpose: SC1 (troca sem recarregar visivelmente) e SC5 (tradução adicional baixável sob demanda sem novo build, app mantém ~45MB). Torna a versão dinâmica (removendo o `const VERSION = "tb"` fixo de reader.tsx:8) e estabelece a base IDB v2 (stores study/plans/search + meta downloadedVersions) que os planos 02-02 e 02-03 consomem.
Output: seletor de tradução no header, URL ?v= como estado navegável, BLIVRE embarcada mas fora do precache, download com progresso para o store chapters, meta downloadedVersions honesta (Pitfall 5).
</objective>

<execution_context>
@C:/Users/VCM37/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/VCM37/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-estudo-e-personaliza-o/02-CONTEXT.md
@.planning/phases/02-estudo-e-personaliza-o/02-RESEARCH.md
@src/lib/bible.ts
@src/components/reader.tsx
@src/components/book-picker.tsx
@src/lib/settings.ts
@next.config.ts
@scripts/generate-data.mjs
</context>

<tasks>

<task type="tracer" reversibility="reversible">
  <name>Task 1: Troca de traduções instantânea tb↔alm1911 sem reload — D-01, D-02, LEI-02</name>
  <files>src/lib/settings.ts, src/components/version-picker.tsx, src/components/reader.tsx</files>
  <read_first>
    - src/components/reader.tsx (arquivo sendo modificado — remover linha 8 `const VERSION = "tb"`; efeito de carga em linhas 75-100; Header linhas 243-297)
    - src/lib/settings.ts (padrão localStorage com lazy init: readTheme/readFontScale)
    - src/components/book-picker.tsx (padrão bottom-sheet overlay + escHandler + role=dialog)
    - src/lib/bible.ts (assinatura getChapter(versionCode, bookId, chapter) e tipo BibleVersionMeta com code/label/shortLabel)
  </read_first>
  <acceptance_criteria>
    - reader.tsx não contém mais `const VERSION = "tb"` (grep retorna 0 ocorrências de "VERSION =")
    - E2E: abrir João 3 (?b=42&c=2) → picker → selecionar ALM 1911 → título/texto do capítulo muda → URL contém v=alm1911 → localStorage bs-version === "alm1911" → trocar de volta para TB → bs-version === "tb"
    - E2E: troca ocorre sem window.location/reload (posição de rolagem e capítulo permanecem; navegação é estado React)
    - `npm run lint`, `npx tsc --noEmit`, `npm run build` passam
  </acceptance_criteria>
  <action>
    Em src/lib/settings.ts: adicionar constante VERSION_KEY = "bs-version"; exportar readVersion(): string que lê localStorage bs-version, valida contra conjunto ["tb","alm1911","blivre"] (inválido → "tb"), default "tb"; exportar writeVersion(code: string) que grava bs-version.
    Criar src/components/version-picker.tsx ("use client", modelo visual de book-picker.tsx — overlay fixed inset-0 z-50 + painel bottom rounded-t-3xl): props { open: boolean; versions: BibleVersionMeta[]; current: string; downloaded: string[]; onSelect: (code: string) => void; onManageDownload: (code: string) => void; onClose: () => void }. Listar index.versions com label + shortLabel; marcar a atual (aria-current + bg-accent); para versões em `downloaded` exibir badge "Baixada"; para "blivre" não baixada exibir botão "Baixar" que chama onManageDownload("blivre"). Mesmo escHandler de Escape de book-picker.
    Em src/components/reader.tsx: substituir `const VERSION = "tb"` (linha 8) por estado `const [version, setVersion] = useState<string>(() => initVersion())` onde initVersion lê ?v= da URL (URLSearchParams) e valida contra index... como index ainda não carregou no lazy init, validar apenas contra o conjunto estático ["tb","alm1911","blivre"], com fallback para readVersion(). Efeito separado: quando `index` carrega, se ?v= presente e válido, sobrescrever com o valor da URL (URL é o estado navegável — D-02). Trocar getChapter(VERSION, ...) por getChapter(version, pos.bookId, pos.chapter) e adicionar version à lista de deps do efeito (linhas 75-100); prefetchAdjacent usa `version`; ao carregar capítulo, montar URL com b, c e v via history.replaceState(null, "", url.toString()) — NUNCA passar objeto URL (Pitfall 4, DataCloneError documentado em STATE.md) — e gravar writeVersion(version). Função handleVersionChange(code): setVersion(code) (o efeito re-renderiza o capítulo automaticamente; sem reload, sem scroll reset além do normal). Passar para o Header/VersionPicker: versions={index.versions}, current={version}, downloaded (estado local, ver Task 3). Não alterar ChapterView nesta task.
  </action>
  <verify>
    <automated>npm run lint; npx tsc --noEmit; npm run build; E2E Playwright: troca tb→alm1911→tb em João 3 com asserção de URL ?v=, texto do capítulo e localStorage bs-version (seguir o padrão de E2E ad-hoc da Fase 1, biblia-e2e.cjs)</automated>
  </verify>
  <done>Usuário troca tb↔alm1911 no mesmo capítulo sem recarregar visivelmente; URL ?v= espelha o estado; bs-version persiste entre sessões; VERSION fixo removido.</done>
</task>

<task type="auto" reversibility="costly">
  <name>Task 2: BLIVRE no pipeline + exclusão do precache — D-03, OFF-02</name>
  <files>scripts/generate-data.mjs, next.config.ts</files>
  <read_first>
    - scripts/generate-data.mjs (VERSIONS linhas 11-14; main() linhas 41-113; assertValidShape linhas 24-39)
    - next.config.ts (collectPublicFiles walk linhas 22-42; comentário linhas 15-20 sobre additionalPrecacheEntries substituir o scan interno)
    - 02-RESEARCH.md §Pitfall 2 (exclusão da BLIVRE é `if` no walk, não glob; cobrir blivre/** E search/blivre)
  </read_first>
  <acceptance_criteria>
    - `node scripts/generate-data.mjs` gera public/data/blivre/ com exatamente 66 arquivos de livro e index.json com versions.length === 3 (tb, alm1911, blivre com code "blivre", label "Bíblia Livre", shortLabel "BLIVRE")
    - public/sw.js gerado no build NÃO contém a string "data/blivre" (node -e lê public/sw.js e assere ausência — nem livros nem search/blivre)
    - `npm run build` conclui; precache reportado não lista entradas blivre
  </acceptance_criteria>
  <action>
    Em scripts/generate-data.mjs: adicionar { code: "blivre", label: "Bíblia Livre", shortLabel: "BLIVRE" } ao array VERSIONS (linhas 11-14). O raw data/raw/BLIVRE.json (~3,9MB, 66 livros) já existe — o pipeline existente lê `${version.code.toUpperCase()}.json`, valida com assertValidShape e escreve public/data/blivre/{abbrev}.json com o mesmo payload { chapters: [...] }; index.json automaticamente ganha a 3ª versão (metaByCode). Não alterar mais nada no pipeline.
    Em next.config.ts, dentro do walk() de collectPublicFiles (após computar childRel, antes do push de entries): adicionar guard `if (childRel.startsWith("blivre") || childRel.startsWith("search/blivre")) { continue; }` — cobre public/data/blivre/** e public/data/search/blivre.json (que o plano 02-02 gerará; o filtro já fica pronto). NÃO usar globIgnores (API injectManifest, não aplicável — RESEARCH Decision central 2). Nenhuma outra mudança na config.
  </action>
  <verify>
    <automated>node scripts/generate-data.mjs; node -e "const idx=require('./public/data/index.json'); if(idx.versions.length!==3||!idx.versions.some(v=>v.code==='blivre'))process.exit(1)"; npm run build; node -e "const sw=require('fs').readFileSync('./public/sw.js','utf8'); if(sw.includes('data/blivre'))process.exit(1)"</automated>
  </verify>
  <done>BLIVRE embarcada em public/data/blivre/** (66 livros), index.json lista 3 traduções, Service Worker precache NÃO contém BLIVRE (D-03, app ~45MB mantido).</done>
</task>

<task type="auto" reversibility="costly">
  <name>Task 3: IDB v2 + downloadTranslation + modal de progresso + status no seletor — OFF-02, D-03</name>
  <files>src/lib/bible.ts, src/components/download-modal.tsx, src/components/version-picker.tsx, src/components/reader.tsx</files>
  <read_first>
    - src/lib/bible.ts (DB_NAME/DB_VERSION linhas 29-30; openDb/onupgradeneeded linhas 52-80; readMeta/writeMeta linhas 95-111; clearChapters linhas 113-120; ensureDataVersion linhas 127-133; loadBookIntoStore linhas 135-163; getChapter linhas 190-211)
    - src/components/book-picker.tsx (padrão visual de modal/overlay para download-modal)
    - 02-RESEARCH.md §Pattern 2 (downloadTranslation reusando loadBookIntoStore) e §Pitfall 5 (ensureDataVersion apaga BLIVRE sem limpar downloadedVersions → UI mente)
  </read_first>
  <acceptance_criteria>
    - bible.ts: DB_VERSION === 2; onupgradeneeded cria (se ausentes) stores "study", "plans", "search"; meta key "downloadedVersions" escrita como JSON array de códigos
    - E2E: picker → BLIVRE mostra "Baixar" → modal com progresso → chega a 66/66 → badge "Baixada" → abrir BLIVRE Gênesis 1 renderiza o capítulo → reload offline (contexto offline do Playwright) mantém o capítulo → meta downloadedVersions contém "blivre"
    - E2E: após download, getDownloadedVersions() retorna array contendo "blivre" (asserção via console/IDB no browser)
    - `npx tsc --noEmit` e `npm run build` passam
  </acceptance_criteria>
  <action>
    Em src/lib/bible.ts:
    (1) DB_VERSION 1 → 2; no onupgradeneeded, criar stores "study" (keyPath "id"), "plans" (keyPath "planId"), "search" (keyPath "versionCode") se objectStoreNames não os contiver — idempotente.
    (2) Adicionar const DOWNLOADED_KEY = "downloadedVersions"; exportar getDownloadedVersions(): Promise<string[]> (readMeta, JSON.parse com try/catch → []).
    (3) Exportar putSearchIndex(versionCode: string, json: string) e getSearchIndex(versionCode: string): Promise<string | null> (store "search", keyPath "versionCode"; o índice serializado do plano 02-02 fica disponível offline no IDB).
    (4) Exportar downloadTranslation(versionCode: string, onProgress: (done: number, total: number) => void): Promise<void> — getIndex() → achar version em index.versions (throw "Versão desconhecida: X" se ausente) → openDb() → loop index.books (66) com await loadBookIntoStore(db, versionCode, book) (put idempotente — Fase 1) → onProgress(++done, 66) a cada livro → após os livros, tentar fetch /data/search/${versionCode}.json; se res.ok, putSearchIndex(versionCode, texto bruto); se 404/erro, SKIP silencioso (o índice ainda não foi gerado — é gerado no plano 02-02; Pitfall 6) → escrever writeMeta(db, DOWNLOADED_KEY, JSON.stringify([...novo set com versionCode])).
    (5) ensureDataVersion (linhas 127-133): além de clearChapters, limpar store "search" (clear) e re-escrever writeMeta(DOWNLOADED_KEY, "[]") quando dataVersion mudar — Pitfall 5: UI nunca diz "baixada" para dados apagados; BLIVRE vira cache versionado (badge some, usuário re-baixa).
    Criar src/components/download-modal.tsx ("use client", mesmo padrão overlay/bottom-sheet de book-picker): props { open, versionCode, onClose, onDone }. Ao abrir, chamar downloadTranslation(versionCode, (done, total) => setProgress({done, total})); renderizar barra de progresso (div com width percentual), texto "Baixando Bíblia Livre… N/66", estado "Concluído — agora disponível offline" ao final, estado de erro com botão "Tentar novamente" (re-chama downloadTranslation); feature-detect navigator.storage.estimate() para exibir "Espaço em uso ~X MB" quando disponível (ocultar se ausente — A6); botão fechar. NÃO precachear nada (D-07 é para dados de usuário; aqui é dado de corpus baixado — IDB é a fonte offline).
    Em version-picker.tsx: receber downloaded: string[] e onManageDownload; botão "Baixar" na linha da BLIVRE quando não baixada; badge "Baixada" quando baixada.
    Em reader.tsx: manter estado local downloaded: string[] (carregado de getDownloadedVersions() no mount); wire do download-modal (open quando onManageDownload("blivre")); ao concluir download, atualizar downloaded e, se o usuário trocar para "blivre", o getChapter existente já resolve (Pitfall 6: garantir loadBookIntoStore da versão ativa no swap — o próprio getChapter faz fetch-on-miss quando online; para blivre após download, o dado já está no IDB).
  </action>
  <verify>
    <automated>npx tsc --noEmit; npm run build; E2E Playwright: fluxo completo de download (modal → 66/66 → badge) + navegação para BLIVRE Gn 1 + reload em contexto offline com 0 erros de console (padrão Fase 1)</automated>
  </verify>
  <done>BLIVRE baixável sob demanda para o store chapters com progresso visível; leitura offline do capítulo após download; downloadedVersions/search limpos junto com chapters em nova dataVersion (Pitfall 5).</done>
</task>

</tasks>

## Flagged Assumptions (edge probe — specless fallback)

| Requirement | Edge | Resolution |
|-------------|------|------------|
| LEI-02 | Troca sem reload com estado navegável | Resolvido explícito — Task 1 (SC1) + Pitfall 4 (replaceState string) |
| OFF-02 | Leitura offline pós-download e pós-update | Resolvido explícito (Task 3) + backstop: honestidade do badge em dataVersion nova (Pitfall 5) — registrado em must_haves.truths com verification: backstop |

Breadcrumb canônico (canon-referral, sem mint de prohibition): XSS via conteúdo — todo corpus/hino/dicionário é renderizado como texto puro via React (sem dangerouslySetInnerHTML); cobre anotações e snippets (plano 02-02/02-03 aplicam a mesma regra).

## Artifacts this phase produces

- `src/lib/settings.ts` — funções `readVersion` / `writeVersion` (chave localStorage `bs-version`)
- `src/lib/bible.ts` — `DB_VERSION = 2`; stores `study`, `plans`, `search`; meta `downloadedVersions`; funções `downloadTranslation(versionCode, onProgress)`, `getDownloadedVersions()`, `putSearchIndex(versionCode, json)`, `getSearchIndex(versionCode)`; `ensureDataVersion` estendido (limpa `search` + `downloadedVersions`)
- `src/components/version-picker.tsx` — NOVO: seletor de tradução no header
- `src/components/download-modal.tsx` — NOVO: modal de progresso do download
- `src/components/reader.tsx` — versão dinâmica via `useState` + URL `?v=` + `bs-version`
- `scripts/generate-data.mjs` — entrada BLIVRE em VERSIONS
- `next.config.ts` — filtro `startsWith("blivre")` / `startsWith("search/blivre")` no walk
- Dados gerados: `public/data/blivre/**` (66 arquivos), `public/data/index.json` (3 versões), `public/sw.js` (precache SEM blivre)

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→/data/ estáticos | Primeira parte, first-party estáticos (BLIVRE servida só quando online; IDB é a fonte offline) |
| client→IndexedDB | Dados locais do usuário (chapters + meta) — sem autenticação nesta fase |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01-01 | Tampering | public/data/blivre/** + search/blivre.json | low | mitigate | Conteúdo estático first-party; defaultCache do Serwist roteia *.json → NetworkFirst; renderizado como texto puro no client |
| T-02-01-02 | Tampering | IndexedDB (chapters/meta) | low | accept | Dados locais sem valor de ataque nesta fase; sync com auth/RLS é Fase 3 (fora de escopo) |
| T-02-01-SC | Tampering | npm installs (minisearch no plano 02-02) | high | mitigate | Package Legitimacy Audit em 02-RESEARCH.md: minisearch `OK`/Approved (8 anos, 2M downloads/sem, sem postinstall, repo lucaong/minisearch) — sem checkpoint bloqueante; pin `^7.2.0` |
</threat_model>

<verification>
- `npm run lint`, `npx tsc --noEmit`, `npm run build` — todos PASS após cada task
- Asserções de dados: index.json com 3 versões; public/data/blivre/ com 66 arquivos; public/sw.js sem "data/blivre"
- E2E Playwright (padrão Fase 1): swap tb↔alm1911 (URL ?v= + bs-version + texto), download BLIVRE (modal 66/66 + badge), leitura BLIVRE offline (reload sem rede, 0 erros console)
</verification>

<success_criteria>
- SC1: troca de tradução no mesmo capítulo sem recarregar visivelmente — VERIFICADO por E2E (texto muda, URL ?v=, bs-version persiste, sem reload)
- SC5: BLIVRE baixada sob demanda (66 livros → IDB chapters) e lida offline — VERIFICADO por E2E offline reload
- D-03: BLIVRE fora do precache — VERIFICADO por asserção em public/sw.js
- Pitfall 5: badge honesto pós-update — backstop registrado
</success_criteria>

<output>
Create `.planning/phases/02-estudo-e-personaliza-o/02-01-SUMMARY.md` when done
</output>

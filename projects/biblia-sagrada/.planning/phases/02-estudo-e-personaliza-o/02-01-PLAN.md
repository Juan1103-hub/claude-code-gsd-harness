---
phase: 02-estudo-e-personaliza-o
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/generate-data.mjs
  - next.config.ts
  - src/lib/settings.ts
  - src/lib/bible.ts
  - src/components/reader.tsx
  - src/components/version-picker.tsx
  - src/components/download-modal.tsx
autonomous: true
requirements: [LEI-02, OFF-02]
estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "UsuÃ¡rio troca de traduÃ§Ã£o (tb â†” alm1911) no mesmo capÃ­tulo sem recarregar visivelmente â€” texto do capÃ­tulo muda, URL ganha ?v=<code> e a escolha persiste entre sessÃµes em localStorage (bs-version) [LEI-02, D-01, D-02, SC1]"
    - "BLIVRE aparece no seletor de traduÃ§Ã£o, Ã© baixÃ¡vel sob demanda (66 livros â†’ IndexedDB chapters) e legÃ­vel offline apÃ³s o download [OFF-02, D-03, SC5]"
    - "BLIVRE (public/data/blivre/**) e search/blivre nÃ£o entram no precache do Service Worker â€” precache continua ~45MB [D-03, Pitfall 2]"
    - statement: "ApÃ³s novo build (dataVersion muda), a UI nÃ£o mente sobre BLIVRE baixada â€” ensureDataVersion limpa downloadedVersions + store search junto com chapters [Pitfall 5]"
      verification: backstop
  artifacts:
    - path: "src/lib/settings.ts"
      provides: "readVersion/writeVersion (localStorage bs-version)"
      contains: "bs-version"
    - path: "src/lib/bible.ts"
      provides: "DB_VERSION=2 + stores study/plans/search + meta downloadedVersions + downloadTranslation + getDownloadedVersions + putSearchIndex/getSearchIndex"
      contains: "DB_VERSION = 2"
    - path: "src/components/version-picker.tsx"
      provides: "Seletor de traduÃ§Ã£o no header (menu bottom-sheet, padrÃ£o book-picker)"
      min_lines: 40
    - path: "src/components/download-modal.tsx"
      provides: "Modal de progresso do download BLIVRE (done/66)"
      min_lines: 40
    - path: "src/components/reader.tsx"
      provides: "VersÃ£o dinÃ¢mica via estado React (removido const VERSION = \"tb\") + URL ?v= + localStorage"
      contains: "useState"
    - path: "scripts/generate-data.mjs"
      provides: "BLIVRE em public/data/blivre/** + index.json com 3 versÃµes"
      contains: "blivre"
    - path: "next.config.ts"
      provides: "Filtro no walk de collectPublicFiles (exclui blivre + search/blivre)"
      contains: "startsWith(\"blivre\")"
    - path: "public/data/blivre/**"
      provides: "66 arquivos de livro BLIVRE embarcados, NÃƒO precacheados"
      min_lines: 1
  key_links:
    - from: "src/components/reader.tsx"
      to: "src/lib/bible.ts"
      via: "getChapter(version, bookId, chapter) â€” versÃ£o dinÃ¢mica em vez de VERSION fixo"
      pattern: "getChapter\\(version"
    - from: "src/components/reader.tsx"
      to: "URL"
      via: "history.replaceState(null, \"\", url.toString()) com ?v= (nunca objeto URL â€” Pitfall 4)"
      pattern: "replaceState\\(null, \"\", url\\.toString\\(\\)\\)"
    - from: "src/components/version-picker.tsx"
      to: "src/lib/bible.ts"
      via: "getDownloadedVersions() para badge de status e disparo do download"
      pattern: "getDownloadedVersions"
    - from: "next.config.ts"
      to: "public/sw.js"
      via: "walk() de collectPublicFiles ignora blivre â†’ precache sem BLIVRE"
      pattern: "startsWith\\(\"blivre\"\\)"
    - from: "src/lib/bible.ts"
      to: "src/lib/bible.ts"
      via: "downloadTranslation â†’ loadBookIntoStore (mesmo primitivo da Fase 1) por livro, idempotente"
      pattern: "loadBookIntoStore"
---

<objective>
Troca de traduÃ§Ãµes instantÃ¢nea no leitor + download sob demanda da BÃ­blia Livre (BLIVRE) para IndexedDB, com leitura 100% offline.

Purpose: SC1 (troca sem recarregar visivelmente) e SC5 (traduÃ§Ã£o adicional baixÃ¡vel sob demanda sem novo build, app mantÃ©m ~45MB). Torna a versÃ£o dinÃ¢mica (removendo o `const VERSION = "tb"` fixo de reader.tsx:8) e estabelece a base IDB v2 (stores study/plans/search + meta downloadedVersions) que os planos 02-02 e 02-03 consomem.
Output: seletor de traduÃ§Ã£o no header, URL ?v= como estado navegÃ¡vel, BLIVRE embarcada mas fora do precache, download com progresso para o store chapters, meta downloadedVersions honesta (Pitfall 5).
</objective>

<execution_context>
@~/.config/opencode/gsd-core/workflows/execute-plan.md
@~/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-estudo-e-personaliza-o/02-CONTEXT.md
@.planning/phases/02-estudo-e-personaliza-o/02-RESEARCH.md
@src/lib/bible.ts
@src/components/reader.tsx
@src/components/book-picker.tsx
@src/lib/settings.ts
@next.config.ts
@scripts/generate-data.mjs
</context>

<tasks>

<task type="tracer" reversibility="reversible">
  <name>Task 1: Troca de traduÃ§Ãµes instantÃ¢nea tbâ†”alm1911 sem reload â€” D-01, D-02, LEI-02</name>
  <files>src/lib/settings.ts, src/components/version-picker.tsx, src/components/reader.tsx</files>
  <read_first>
    - src/components/reader.tsx (arquivo sendo modificado â€” remover linha 8 `const VERSION = "tb"`; efeito de carga em linhas 75-100; Header linhas 243-297)
    - src/lib/settings.ts (padrÃ£o localStorage com lazy init: readTheme/readFontScale)
    - src/components/book-picker.tsx (padrÃ£o bottom-sheet overlay + escHandler + role=dialog)
    - src/lib/bible.ts (assinatura getChapter(versionCode, bookId, chapter) e tipo BibleVersionMeta com code/label/shortLabel)
    - node_modules/next/dist/docs/ (regra Next.js 16 do AGENTS.md do projeto â€” ler o guia relevante de client components antes de codar reader/version-picker; heed deprecation notices)
  </read_first>
  <acceptance_criteria>
    - reader.tsx nÃ£o contÃ©m mais `const VERSION = "tb"` (grep retorna 0 ocorrÃªncias de "VERSION =")
    - E2E: abrir JoÃ£o 3 (?b=42&c=2) â†’ picker â†’ selecionar ALM 1911 â†’ tÃ­tulo/texto do capÃ­tulo muda â†’ URL contÃ©m v=alm1911 â†’ localStorage bs-version === "alm1911" â†’ trocar de volta para TB â†’ bs-version === "tb"
    - E2E: troca ocorre sem window.location/reload (posiÃ§Ã£o de rolagem e capÃ­tulo permanecem; navegaÃ§Ã£o Ã© estado React)
    - `npm run lint`, `npx tsc --noEmit`, `npm run build` passam
  </acceptance_criteria>
  <action>
    Em src/lib/settings.ts: adicionar constante VERSION_KEY = "bs-version"; exportar readVersion(): string que lÃª localStorage bs-version, valida contra conjunto ["tb","alm1911","blivre"] (invÃ¡lido â†’ "tb"), default "tb"; exportar writeVersion(code: string) que grava bs-version.
    Criar src/components/version-picker.tsx ("use client", modelo visual de book-picker.tsx â€” overlay fixed inset-0 z-50 + painel bottom rounded-t-3xl): props { open: boolean; versions: BibleVersionMeta[]; current: string; downloaded: string[]; onSelect: (code: string) => void; onManageDownload: (code: string) => void; onClose: () => void }. Listar index.versions com label + shortLabel; marcar a atual (aria-current + bg-accent); para versÃµes em `downloaded` exibir badge "Baixada"; para "blivre" nÃ£o baixada exibir botÃ£o "Baixar" que chama onManageDownload("blivre"). Mesmo escHandler de Escape de book-picker.
    Em src/components/reader.tsx: substituir `const VERSION = "tb"` (linha 8) por estado `const [version, setVersion] = useState<string>(() => initVersion())` onde initVersion lÃª ?v= da URL (URLSearchParams) e valida contra index... como index ainda nÃ£o carregou no lazy init, validar apenas contra o conjunto estÃ¡tico ["tb","alm1911","blivre"], com fallback para readVersion(). Efeito separado: quando `index` carrega, se ?v= presente e vÃ¡lido, sobrescrever com o valor da URL (URL Ã© o estado navegÃ¡vel â€” D-02). Trocar getChapter(VERSION, ...) por getChapter(version, pos.bookId, pos.chapter) e adicionar version Ã  lista de deps do efeito (linhas 75-100); prefetchAdjacent usa `version`; ao carregar capÃ­tulo, montar URL com b, c e v via history.replaceState(null, "", url.toString()) â€” NUNCA passar objeto URL (Pitfall 4, DataCloneError documentado em STATE.md) â€” e gravar writeVersion(version). FunÃ§Ã£o handleVersionChange(code): setVersion(code) (o efeito re-renderiza o capÃ­tulo automaticamente; sem reload, sem scroll reset alÃ©m do normal). Passar para o Header/VersionPicker: versions={index.versions}, current={version}, downloaded (estado local, ver Task 3). NÃ£o alterar ChapterView nesta task.
  </action>
  <verify>
    <automated>npm run lint; npx tsc --noEmit; npm run build; E2E Playwright: troca tbâ†’alm1911â†’tb em JoÃ£o 3 com asserÃ§Ã£o de URL ?v=, texto do capÃ­tulo e localStorage bs-version (seguir o padrÃ£o de E2E ad-hoc da Fase 1, biblia-e2e.cjs)</automated>
  </verify>
  <done>UsuÃ¡rio troca tbâ†”alm1911 no mesmo capÃ­tulo sem recarregar visivelmente; URL ?v= espelha o estado; bs-version persiste entre sessÃµes; VERSION fixo removido.</done>
</task>

<task type="auto" reversibility="costly">
  <name>Task 2: BLIVRE no pipeline + exclusÃ£o do precache â€” D-03, OFF-02</name>
  <files>scripts/generate-data.mjs, next.config.ts</files>
  <read_first>
    - scripts/generate-data.mjs (VERSIONS linhas 11-14; main() linhas 41-113; assertValidShape linhas 24-39)
    - next.config.ts (collectPublicFiles walk linhas 22-42; comentÃ¡rio linhas 15-20 sobre additionalPrecacheEntries substituir o scan interno)
    - 02-RESEARCH.md Â§Pitfall 2 (exclusÃ£o da BLIVRE Ã© `if` no walk, nÃ£o glob; cobrir blivre/** E search/blivre)
    - node_modules/next/dist/docs/ (regra Next.js 16 do AGENTS.md do projeto â€” conferir doc de config antes de alterar next.config.ts; heed deprecation notices)
  </read_first>
  <acceptance_criteria>
    - `node scripts/generate-data.mjs` gera public/data/blivre/ com exatamente 66 arquivos de livro e index.json com versions.length === 3 (tb, alm1911, blivre com code "blivre", label "BÃ­blia Livre", shortLabel "BLIVRE")
    - public/sw.js gerado no build NÃƒO contÃ©m "data/blivre" nem "search/blivre" (node -e lÃª public/sw.js com DUAS asserÃ§Ãµes â€” cobre public/data/blivre/** E o Ã­ndice public/data/search/blivre.json)
    - `npm run build` conclui; precache reportado nÃ£o lista entradas blivre
  </acceptance_criteria>
  <action>
    Em scripts/generate-data.mjs: adicionar { code: "blivre", label: "BÃ­blia Livre", shortLabel: "BLIVRE" } ao array VERSIONS (linhas 11-14). O raw data/raw/BLIVRE.json (~3,9MB, 66 livros) jÃ¡ existe â€” o pipeline existente lÃª `${version.code.toUpperCase()}.json`, valida com assertValidShape e escreve public/data/blivre/{abbrev}.json com o mesmo payload { chapters: [...] }; index.json automaticamente ganha a 3Âª versÃ£o (metaByCode). NÃ£o alterar mais nada no pipeline.
    Em next.config.ts, dentro do walk() de collectPublicFiles (apÃ³s computar childRel, antes do push de entries): adicionar guard `if (childRel.startsWith("blivre") || childRel.startsWith("search/blivre")) { continue; }` â€” cobre public/data/blivre/** e public/data/search/blivre.json (que o plano 02-02 gerarÃ¡; o filtro jÃ¡ fica pronto). NÃƒO usar globIgnores (API injectManifest, nÃ£o aplicÃ¡vel â€” RESEARCH Decision central 2). Nenhuma outra mudanÃ§a na config.
  </action>
  <verify>
    <automated>node scripts/generate-data.mjs; node -e "const idx=require('./public/data/index.json'); if(idx.versions.length!==3||!idx.versions.some(v=>v.code==='blivre'))process.exit(1)"; npm run build; node -e "const sw=require('fs').readFileSync('./public/sw.js','utf8'); if(sw.includes('data/blivre')||sw.includes('search/blivre'))process.exit(1)"</automated>
  </verify>
  <done>BLIVRE embarcada em public/data/blivre/** (66 livros), index.json lista 3 traduÃ§Ãµes, Service Worker precache NÃƒO contÃ©m BLIVRE (D-03, app ~45MB mantido).</done>
</task>

<task type="auto" reversibility="costly">
  <name>Task 3: IDB v2 + downloadTranslation + modal de progresso + status no seletor â€” OFF-02, D-03</name>
  <files>src/lib/bible.ts, src/components/download-modal.tsx, src/components/version-picker.tsx, src/components/reader.tsx</files>
  <read_first>
    - src/lib/bible.ts (DB_NAME/DB_VERSION linhas 29-30; openDb/onupgradeneeded linhas 52-80; readMeta/writeMeta linhas 95-111; clearChapters linhas 113-120; ensureDataVersion linhas 127-133; loadBookIntoStore linhas 135-163; getChapter linhas 190-211)
    - src/components/book-picker.tsx (padrÃ£o visual de modal/overlay para download-modal)
    - 02-RESEARCH.md Â§Pattern 2 (downloadTranslation reusando loadBookIntoStore) e Â§Pitfall 5 (ensureDataVersion apaga BLIVRE sem limpar downloadedVersions â†’ UI mente)
  </read_first>
  <acceptance_criteria>
    - bible.ts: DB_VERSION === 2; onupgradeneeded cria (se ausentes) stores "study", "plans", "search"; meta key "downloadedVersions" escrita como JSON array de cÃ³digos
    - E2E: picker â†’ BLIVRE mostra "Baixar" â†’ modal com progresso â†’ chega a 66/66 â†’ badge "Baixada" â†’ abrir BLIVRE GÃªnesis 1 renderiza o capÃ­tulo â†’ reload offline (contexto offline do Playwright) mantÃ©m o capÃ­tulo â†’ meta downloadedVersions contÃ©m "blivre"
    - E2E: apÃ³s download, getDownloadedVersions() retorna array contendo "blivre" (asserÃ§Ã£o via console/IDB no browser)
    - `npx tsc --noEmit` e `npm run build` passam
  </acceptance_criteria>
  <action>
    Em src/lib/bible.ts:
    (1) DB_VERSION 1 â†’ 2; no onupgradeneeded, criar stores "study" (keyPath "id"), "plans" (keyPath "planId"), "search" (keyPath "versionCode") se objectStoreNames nÃ£o os contiver â€” idempotente.
    (2) Adicionar const DOWNLOADED_KEY = "downloadedVersions"; exportar getDownloadedVersions(): Promise<string[]> (readMeta, JSON.parse com try/catch â†’ []).
    (3) Exportar putSearchIndex(versionCode: string, json: string) e getSearchIndex(versionCode: string): Promise<string | null> (store "search", keyPath "versionCode"; o Ã­ndice serializado do plano 02-02 fica disponÃ­vel offline no IDB).
    (4) Exportar downloadTranslation(versionCode: string, onProgress: (done: number, total: number) => void): Promise<void> â€” getIndex() â†’ achar version em index.versions (throw "VersÃ£o desconhecida: X" se ausente) â†’ openDb() â†’ loop index.books (66) com await loadBookIntoStore(db, versionCode, book) (put idempotente â€” Fase 1) â†’ onProgress(++done, 66) a cada livro â†’ apÃ³s os livros, tentar fetch /data/search/${versionCode}.json; se res.ok, putSearchIndex(versionCode, texto bruto); se 404/erro, SKIP silencioso (o Ã­ndice ainda nÃ£o foi gerado â€” Ã© gerado no plano 02-02; Pitfall 6) â†’ escrever writeMeta(db, DOWNLOADED_KEY, JSON.stringify([...novo set com versionCode])).
    (5) ensureDataVersion (linhas 127-133): alÃ©m de clearChapters, limpar store "search" (clear) e re-escrever writeMeta(DOWNLOADED_KEY, "[]") quando dataVersion mudar â€” Pitfall 5: UI nunca diz "baixada" para dados apagados; BLIVRE vira cache versionado (badge some, usuÃ¡rio re-baixa).
    Criar src/components/download-modal.tsx ("use client", mesmo padrÃ£o overlay/bottom-sheet de book-picker): props { open, versionCode, onClose, onDone }. Ao abrir, chamar downloadTranslation(versionCode, (done, total) => setProgress({done, total})); renderizar barra de progresso (div com width percentual), texto "Baixando BÃ­blia Livreâ€¦ N/66", estado "ConcluÃ­do â€” agora disponÃ­vel offline" ao final, estado de erro com botÃ£o "Tentar novamente" (re-chama downloadTranslation); feature-detect navigator.storage.estimate() para exibir "EspaÃ§o em uso ~X MB" quando disponÃ­vel (ocultar se ausente â€” A6); botÃ£o fechar. NÃƒO precachear nada (D-07 Ã© para dados de usuÃ¡rio; aqui Ã© dado de corpus baixado â€” IDB Ã© a fonte offline).
    Em version-picker.tsx: receber downloaded: string[] e onManageDownload; botÃ£o "Baixar" na linha da BLIVRE quando nÃ£o baixada; badge "Baixada" quando baixada.
    Em reader.tsx: manter estado local downloaded: string[] (carregado de getDownloadedVersions() no mount); wire do download-modal (open quando onManageDownload("blivre")); ao concluir download, atualizar downloaded e, se o usuÃ¡rio trocar para "blivre", o getChapter existente jÃ¡ resolve (Pitfall 6: garantir loadBookIntoStore da versÃ£o ativa no swap â€” o prÃ³prio getChapter faz fetch-on-miss quando online; para blivre apÃ³s download, o dado jÃ¡ estÃ¡ no IDB).
  </action>
  <verify>
    <automated>npx tsc --noEmit; npm run build; E2E Playwright: fluxo completo de download (modal â†’ 66/66 â†’ badge) + navegaÃ§Ã£o para BLIVRE Gn 1 + reload em contexto offline com 0 erros de console (padrÃ£o Fase 1)</automated>
  </verify>
  <done>BLIVRE baixÃ¡vel sob demanda para o store chapters com progresso visÃ­vel; leitura offline do capÃ­tulo apÃ³s download; downloadedVersions/search limpos junto com chapters em nova dataVersion (Pitfall 5).</done>
</task>

</tasks>

## Flagged Assumptions (edge probe â€” specless fallback)

| Requirement | Edge | Resolution |
|-------------|------|------------|
| LEI-02 | Troca sem reload com estado navegÃ¡vel | Resolvido explÃ­cito â€” Task 1 (SC1) + Pitfall 4 (replaceState string) |
| OFF-02 | Leitura offline pÃ³s-download e pÃ³s-update | Resolvido explÃ­cito (Task 3) + backstop: honestidade do badge em dataVersion nova (Pitfall 5) â€” registrado em must_haves.truths com verification: backstop |

Breadcrumb canÃ´nico (canon-referral, sem mint de prohibition): XSS via conteÃºdo â€” todo corpus/hino/dicionÃ¡rio Ã© renderizado como texto puro via React (sem dangerouslySetInnerHTML); cobre anotaÃ§Ãµes e snippets (plano 02-02/02-03 aplicam a mesma regra).

## Prohibitions

- Nunca marcar progresso de plano de leitura automaticamente â€” progresso sÃ³ muda por aÃ§Ã£o explÃ­cita do usuÃ¡rio (EST-05; implementado no 02-03 T3). Status: flagged â€” E2E deve provar que abrir/ler leitura nÃ£o marca dia.
- Nunca usar dangerouslySetInnerHTML â€” todo conteÃºdo (corpus, hinos, dicionÃ¡rio, anotaÃ§Ãµes, snippets de busca) Ã© renderizado como texto puro via React (XSS canÃ´nico, V5 ASVS). Status: flagged â€” grep no verify (`grep -rn "dangerouslySetInnerHTML" src/` deve retornar 0).

## Artifacts this phase produces

- `src/lib/settings.ts` â€” funÃ§Ãµes `readVersion` / `writeVersion` (chave localStorage `bs-version`)
- `src/lib/bible.ts` â€” `DB_VERSION = 2`; stores `study`, `plans`, `search`; meta `downloadedVersions`; funÃ§Ãµes `downloadTranslation(versionCode, onProgress)`, `getDownloadedVersions()`, `putSearchIndex(versionCode, json)`, `getSearchIndex(versionCode)`; `ensureDataVersion` estendido (limpa `search` + `downloadedVersions`)
- `src/components/version-picker.tsx` â€” NOVO: seletor de traduÃ§Ã£o no header
- `src/components/download-modal.tsx` â€” NOVO: modal de progresso do download
- `src/components/reader.tsx` â€” versÃ£o dinÃ¢mica via `useState` + URL `?v=` + `bs-version`
- `scripts/generate-data.mjs` â€” entrada BLIVRE em VERSIONS
- `next.config.ts` â€” filtro `startsWith("blivre")` / `startsWith("search/blivre")` no walk
- Dados gerados: `public/data/blivre/**` (66 arquivos), `public/data/index.json` (3 versÃµes), `public/sw.js` (precache SEM blivre)

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| clientâ†’/data/ estÃ¡ticos | Primeira parte, first-party estÃ¡ticos (BLIVRE servida sÃ³ quando online; IDB Ã© a fonte offline) |
| clientâ†’IndexedDB | Dados locais do usuÃ¡rio (chapters + meta) â€” sem autenticaÃ§Ã£o nesta fase |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01-01 | Tampering | public/data/blivre/** + search/blivre.json | low | mitigate | ConteÃºdo estÃ¡tico first-party; defaultCache do Serwist roteia *.json â†’ NetworkFirst; renderizado como texto puro no client |
| T-02-01-02 | Tampering | IndexedDB (chapters/meta) | low | accept | Dados locais sem valor de ataque nesta fase; sync com auth/RLS Ã© Fase 3 (fora de escopo) |
| T-02-01-SC | Tampering | npm installs (minisearch no plano 02-02) | high | mitigate | Package Legitimacy Audit em 02-RESEARCH.md: minisearch `OK`/Approved (8 anos, 2M downloads/sem, sem postinstall, repo lucaong/minisearch) â€” sem checkpoint bloqueante; pin `^7.2.0` |
</threat_model>

<verification>
- `npm run lint`, `npx tsc --noEmit`, `npm run build` â€” todos PASS apÃ³s cada task
- AsserÃ§Ãµes de dados: index.json com 3 versÃµes; public/data/blivre/ com 66 arquivos; public/sw.js sem "data/blivre"
- E2E Playwright (padrÃ£o Fase 1): swap tbâ†”alm1911 (URL ?v= + bs-version + texto), download BLIVRE (modal 66/66 + badge), leitura BLIVRE offline (reload sem rede, 0 erros console)
</verification>

<success_criteria>
- SC1: troca de traduÃ§Ã£o no mesmo capÃ­tulo sem recarregar visivelmente â€” VERIFICADO por E2E (texto muda, URL ?v=, bs-version persiste, sem reload)
- SC5: BLIVRE baixada sob demanda (66 livros â†’ IDB chapters) e lida offline â€” VERIFICADO por E2E offline reload
- D-03: BLIVRE fora do precache â€” VERIFICADO por asserÃ§Ã£o em public/sw.js
- Pitfall 5: badge honesto pÃ³s-update â€” backstop registrado
</success_criteria>

<output>
Create `.planning/phases/02-estudo-e-personaliza-o/02-01-SUMMARY.md` when done
</output>

---
phase: 02-estudo-e-personaliza-o
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - package.json
  - scripts/search-build.mjs
  - scripts/study-build.mjs
  - data/raw/dictionary.json
  - data/raw/themes.json
  - src/lib/search-options.ts
  - src/lib/search.ts
  - src/lib/study.ts
  - src/components/search-view.tsx
  - src/components/study-view.tsx
  - app/page.tsx
autonomous: true
requirements: [LEI-04, EST-04, EST-06]
estimate:
  tokens: 65000
  raw_tokens: 65000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "UsuÃ¡rio busca palavra e encontra versÃ­culos em <1s, offline, com Ã­ndice FTS prÃ©-computado por traduÃ§Ã£o (MiniSearch 7.2.0) [LEI-04, D-04, SC2]"
    - "Resultado da busca Ã© nÃ­vel versÃ­culo com contexto e destaque (<mark>), e o clique navega para o capÃ­tulo via ?b=&c=&v= [D-05]"
    - "Busca Ã© escopada por traduÃ§Ã£o â€” Ã­ndice por versÃ£o; BLIVRE sÃ³ pesquisÃ¡vel apÃ³s baixada (ou online) [D-05, Pitfall 7]"
    - "UsuÃ¡rio consulta dicionÃ¡rio bÃ­blico integrado, offline, com ~100-300 verbetes curados [EST-04, D-08, A1]"
    - "UsuÃ¡rio acessa a seÃ§Ã£o 'O que a BÃ­blia diz' com temas e versÃ­culos-chave com texto embarcado [EST-06, D-11]"
    - statement: "ConteÃºdo de estudo (dicionÃ¡rio ~100+ verbetes, temas â‰¥10) Ã© produzido e curado durante esta fase â€” dados de entrada em data/raw/** validados e versionados pelo pipeline [A1/OQ-1]"
      verification: backstop
  artifacts:
    - path: "src/lib/search-options.ts"
      provides: "SEARCH_OPTIONS (MiniSearch) + normalizeTerm â€” fonte canÃ´nica, espelhada no build (Pitfall 1)"
      contains: "processTerm"
    - path: "scripts/search-build.mjs"
      provides: "ConstrÃ³i public/data/search/{code}.json por traduÃ§Ã£o no build (2-4MB cada), com log de tamanho (OQ-2)"
      contains: "MiniSearch"
    - path: "src/lib/search.ts"
      provides: "getSearch(versionCode) lazy (memÃ³ria â†’ IDB search store â†’ fetch) + SearchResult + self-heal putSearchIndex"
      contains: "getSearchIndex"
    - path: "src/components/search-view.tsx"
      provides: "View de busca: input, filtro de traduÃ§Ã£o, resultados com <mark>, clique â†’ navega"
      min_lines: 40
    - path: "src/components/study-view.tsx"
      provides: "View Estudo: abas DicionÃ¡rio e 'O que a BÃ­blia diz' (expansÃ­vel no plano 02-03)"
      min_lines: 40
    - path: "app/page.tsx"
      provides: "Shell single-page: tab bar Leitura/Busca/Estudo (sem novas rotas)"
      contains: "Busca"
    - path: "scripts/study-build.mjs"
      provides: "Valida e escreve public/data/study/dictionary.json + themes.json (extensÃ­vel no 02-03 p/ hinos e planos)"
      contains: "dictionary"
    - path: "src/lib/study.ts"
      provides: "Types + getDictionary()/getThemes() (fetch cache); estendido no 02-03 com getHymns/getPlans"
      contains: "getDictionary"
    - path: "data/raw/dictionary.json"
      provides: "~100-300 verbetes {word, definition}"
      min_lines: 100
    - path: "data/raw/themes.json"
      provides: "â‰¥10 temas {id, title, verses:[{book,chapter,verse,text}]}"
      min_lines: 20
    - path: "public/data/search/tb.json"
      provides: "Ãndice MiniSearch TB (~31k docs)"
      min_lines: 1
    - path: "public/data/search/alm1911.json"
      provides: "Ãndice MiniSearch ALM1911 (~31k docs)"
      min_lines: 1
  key_links:
    - from: "src/lib/search.ts"
      to: "src/lib/bible.ts"
      via: "getSearchIndex/putSearchIndex (accessors do store search criados no 02-01)"
      pattern: "getSearchIndex"
    - from: "scripts/search-build.mjs"
      to: "src/lib/search-options.ts"
      via: "mesmo SEARCH_OPTIONS no build e no client (Pitfall 1 â€” loadJSON exige as mesmas options); deep-equal verificado"
      pattern: "SEARCH_OPTIONS"
    - from: "src/lib/search.ts"
      to: "public/data/search/*.json"
      via: "MiniSearch.loadJSON lazy sob demanda, sem fetch de ~31k docs no boot"
      pattern: "loadJSON"
    - from: "src/components/search-view.tsx"
      to: "URL"
      via: "onNavigate(book, chapter, version) â†’ ?b=&c=&v= via replaceState string (Pitfall 4)"
      pattern: "replaceState"
    - from: "scripts/study-build.mjs"
      to: "public/data/study/*.json"
      via: "valida data/raw/** e grava em public/data/study/** (precached â€” D-08 conteÃºdo curado embarcado)"
      pattern: "study"
---

<objective>
Busca FTS no navegador + dicionÃ¡rio integrado + seÃ§Ã£o "O que a BÃ­blia diz".

Purpose: SC2 (busca em <1s sobre ~31k versÃ­culos, offline), EST-04 e EST-06. Ãndice MiniSearch prÃ©-computado no build (1 arquivo por traduÃ§Ã£o em /data/search/{code}.json â€” D-04), carregado lazy no client, e conteÃºdo de estudo curado em /data/study/*.json. Introduz o shell single-page com tab bar Leitura/Busca/Estudo (OQ-3) que o plano 02-03 estende.
Output: busca por versÃ­culo com destaque e navegaÃ§Ã£o, dicionÃ¡rio pesquisÃ¡vel, temas com versÃ­culos-chave embarcados, shell com 3 abas.
</objective>

<execution_context>
@~/.config/opencode/gsd-core/workflows/execute-plan.md
@~/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-estudo-e-personaliza-o/02-CONTEXT.md
@.planning/phases/02-estudo-e-personaliza-o/02-RESEARCH.md
@src/lib/bible.ts
@src/lib/settings.ts
@src/components/reader.tsx
@src/components/book-picker.tsx
@app/page.tsx
@scripts/generate-data.mjs
@package.json
</context>

<tasks>

<task type="tracer" reversibility="costly">
  <name>Task 1: Busca FTS end-to-end â€” MiniSearch, Ã­ndice prÃ©-computado, shell com tab bar â€” LEI-04, D-04, D-05, SC2</name>
  <files>package.json, src/lib/search-options.ts, scripts/search-build.mjs, src/lib/search.ts, src/components/search-view.tsx, app/page.tsx</files>
  <read_first>
    - 02-RESEARCH.md (DecisÃ£o central 1 â€” MiniSearch; Pitfall 1 options idÃªnticas; Pitfall 7 escopo por traduÃ§Ã£o)
    - package.json (scripts prebuild; sem dep de busca ainda)
    - scripts/generate-data.mjs (estrutura de versÃµes/livros gerada por VERSIONS â€” formato de referÃªncia)
    - src/lib/bible.ts (getSearchIndex/putSearchIndex criados no 02-01; BibleIndex com versions)
    - app/page.tsx (hoje renderiza <Reader /> puro)
    - node_modules/next/dist/docs/ (regra Next.js 16 do AGENTS.md do projeto â€” "use client" e shell de pÃ¡gina Ãºnica em app/page.tsx; ler o guia relevante antes de codar; heed deprecation notices)
  </read_first>
  <acceptance_criteria>
    - package.json contÃ©m "minisearch": "^7.2.0" em dependencies; prebuild = "node scripts/generate-data.mjs && node scripts/search-build.mjs && node scripts/study-build.mjs"
    - `npm run build` (executado na Task 2, quando study-build.mjs existe) gera public/data/search/tb.json e public/data/search/alm1911.json; node -e afirma que cada um parseia e representa â‰¥ 31000 versÃ­culos indexados
    - E2E: aba Busca â†’ digitar "amor" â†’ resultados renderizados com <mark> e tempo medido <1000ms â†’ clicar 1Âº resultado â†’ leitor abre com ?b=&c= corretos (e ?v= se filtro de traduÃ§Ã£o ativo)
    - E2E: trocar filtro para ALM 1911 â†’ resultados refletem o Ã­ndice alm1911 (abbrev/label)
    - E2E: nenhum erro de console; sem dangerouslySetInnerHTML no cÃ³digo (grep)
    - `npm run lint`, `npx tsc --noEmit`, `npm run build` passam
  </acceptance_criteria>
  <action>
    Instalar: `npm install minisearch@^7.2.0` (Package Legitimacy Audit OK/Approved em 02-RESEARCH.md â€” sem checkpoint; pin ^7.2.0).
    package.json: prebuild = "node scripts/generate-data.mjs && node scripts/search-build.mjs && node scripts/study-build.mjs" (chain completo sÃ³ vale quando os 3 scripts existirem; study-build Ã© criado na Task 2 â€” por isso o build full da Task 1 roda somente apÃ³s a Task 2).
    Criar src/lib/search-options.ts: export const SEARCH_OPTIONS = { fields: ["text"], storeFields: ["book","abbrev","chapter","verse","text"], idField: "id", processTerm: (t) => { const n = t.normalize("NFD").replace(/[\u0300-\u036f]/g, "").toLowerCase(); const STOP = new Set(["a","de","do","da","em","e","o","os","as","um","uma","que","para","dos","das","nÃ£o","nao","se","com"]); if (STOP.has(n) || !n) return null; return n; } }; export const SEARCH_QUERY_OPTS = { prefix: true, fuzzy: 0.1, combineWith: "AND" }; export function normalizeTerm(t: string): string (mesmo NFD strip + lowercase, sem stopwords). O CLIENT usa esses exports (Pitfall 1 â€” MiniSearch.loadJSON exige as mesmas options usadas no addAll; options divergentes corrompem a busca).
    Criar scripts/search-build.mjs (node): duplicar SEARCH_OPTIONS localmente com o comentÃ¡rio "MANTENHA EM SINCRONIA COM src/lib/search-options.ts" (o projeto nÃ£o tem loader TS para build scripts; a verificaÃ§Ã£o abaixo confere igualdade via deep-equal das duas constantes). Ler public/data/index.json (versions + books); para cada version.code: se !fs.existsSync(public/data/{code}) â†’ console.warn(`skip ${code}`) e continuar (blivre ainda nÃ£o baixada â€” 02-01 Task 3 deixa 404-skip e o self-heal fecha); senÃ£o walk nos arquivos {abbrev}.json â†’ para cada capÃ­tulo e versÃ­culo push doc { id: `${bookId}:${ch}:${v}`, book: bookId, abbrev: book.abbrev, chapter: ch, verse: v, text: verseText }; const ms = new MiniSearch(SEARCH_OPTIONS); ms.addAll(docs); fs.writeFileSync(public/data/search/{code}.json, JSON.stringify(ms)); console.log(`${code}: ${(bytes/1024).toFixed(1)}KB, ${docs.length} docs`) (OQ-2: medir e logar o tamanho).
    Criar src/lib/search.ts: import { SEARCH_OPTIONS, SEARCH_QUERY_OPTS } from "@/lib/search-options"; import { getSearchIndex, putSearchIndex } from "@/lib/bible"; const cache = new Map<string, MiniSearch>(); export interface SearchResult { id: string; score: number; match: Record<string, string[]>; book: number; abbrev: string; chapter: number; verse: number; text: string }; export async function getSearch(versionCode: string): Promise<MiniSearch> â€” ordem: (1) cache em memÃ³ria; (2) await getSearchIndex(versionCode) do IDB â†’ MiniSearch.loadJSON(JSON.parse(saved), SEARCH_OPTIONS) em try/catch; (3) senÃ£o fetch(`/data/search/${versionCode}.json`) â†’ res.ok senÃ£o throw new Error(`Ãndice de busca indisponÃ­vel: ${versionCode}`) â†’ putSearchIndex(versionCode, json) e loadJSON. O passo 3 Ã© o self-heal que cobre BLIVRE baixada em 02-01 sem Ã­ndice: a primeira busca dispara download+persistÃªncia do Ã­ndice.
    Criar src/components/search-view.tsx ("use client"): props { index: BibleIndex; activeVersion: string; onNavigate: (bookId: number, chapter: number, version: string) => void }. Estado: term, version (default activeVersion), results: SearchResult[], loading, searchTimeMs, error. Filtro de traduÃ§Ã£o: <select aria-label="TraduÃ§Ã£o"> com index.versions (label + shortLabel). Submit: const t0 = performance.now(); const ms = await getSearch(version); const res = ms.search(normalizeTerm(term), SEARCH_QUERY_OPTS); setSearchTimeMs(performance.now() - t0); renderizar atÃ© 100 resultados: `abbrev chapter:verse` + texto com <mark> nos termos (split por termo normalizado em JSX â€” nunca dangerouslySetInnerHTML). Clique â†’ onNavigate(r.book, r.chapter, version). Estados: loading, vazio "Nenhum resultado", erro (ex.: blivre sem Ã­ndice â†’ "Baixe a traduÃ§Ã£o para buscar offline").
    app/page.tsx: converter para "use client" shell: estado view "reader"|"search"|"study" (default "reader"); tab bar fixa inferior (OQ-3) com 3 botÃµes Leitura/Busca/Estudo com aria-current; container com padding-bottom para a barra; view "reader" â†’ <Reader />; view "search" â†’ <SearchView index={index} activeVersion={readVersion()} onNavigate={(b,c,v) => { setView("reader"); const url = new URL(window.location.href); url.searchParams.set("b", String(b)); url.searchParams.set("c", String(c)); if (v) url.searchParams.set("v", v); history.replaceState(null, "", url.toString()); }} /> (Pitfall 4 â€” sempre url.toString()); view "study" â†’ placeholder "Em breve" (Task 3 substitui). Carregar index via getIndex() em useEffect (compartilhado entre views; Reader carrega o prÃ³prio, cache interno).
  </action>
  <verify>
    <automated>node scripts/generate-data.mjs; node scripts/search-build.mjs (rodar ANTES do E2E â€” o E2E da busca depende do Ã­ndice public/data/search/{tb,alm1911}.json existir no disco); npm install minisearch@^7.2.0; node -e "const a=require('fs').readFileSync('scripts/search-build.mjs','utf8'),b=require('fs').readFileSync('src/lib/search-options.ts','utf8');const get=c=>{const m=c.match(/SEARCH_OPTIONS\s*=\s*(\{[\s\S]*?\})/);return eval('('+m[1]+')')};if(JSON.stringify(get(a))!==JSON.stringify(get(b)))process.exit(1)"; npm run lint; npx tsc --noEmit; E2E: busca "amor" â†’ <mark> + tempo <1000ms + navegaÃ§Ã£o ?b=&c=; grep -rn "dangerouslySetInnerHTML" src/ (0 ocorrÃªncias)</automated>
  </verify>
  <done>Busca FTS <1s offline com Ã­ndice prÃ©-computado por traduÃ§Ã£o, resultado por versÃ­culo com destaque e navegaÃ§Ã£o para o capÃ­tulo; shell com tab bar Leitura/Busca/Estudo.</done>
</task>

<task type="auto" reversibility="reversible">
  <name>Task 2: ConteÃºdo curado â€” dicionÃ¡rio + 'O que a BÃ­blia diz' â€” EST-04, EST-06, D-08, D-11</name>
  <files>data/raw/dictionary.json, data/raw/themes.json, scripts/study-build.mjs, src/lib/study.ts</files>
  <read_first>
    - 02-CONTEXT.md D-08 (dicionÃ¡rio 200-300 verbetes, mitigaÃ§Ã£o A1 ~100) e D-11 (temas com texto embarcado em JSON)
    - scripts/generate-data.mjs (padrÃ£o de validaÃ§Ã£o/shape â€” replicar em study-build.mjs)
    - scripts/search-build.mjs (se jÃ¡ criado; senÃ£o ignorar â€” script novo)
  </read_first>
  <acceptance_criteria>
    - node -e exec: public/data/study/dictionary.json Ã© array com â‰¥100 objetos {word: string, definition: string} â€” todas word/definition nÃ£o vazias
    - node -e exec: public/data/study/themes.json Ã© array com â‰¥10 objetos {id, title, verses: [{book, chapter, verse, text}]} â€” cada verse com 4 campos preenchidos
    - `npm run build` completo passa com prebuild chain (generate-data + search-build + study-build)
    - src/lib/study.ts exporta getDictionary()/getThemes() com cache; `npm run lint`/`npx tsc --noEmit` passam
  </acceptance_criteria>
  <action>
    data/raw/dictionary.json: curar ~100-300 entradas {word, definition} â€” nomes prÃ³prios e termos essenciais (AbraÃ£o, AdÃ£o, AlianÃ§a, Amor, Anjo, ApÃ³stolo, Arrependimento, Batismo, BÃªnÃ§Ã£o, CÃ©u, Cristo, DiscÃ­pulo, EspÃ­rito Santo, FÃ©, GraÃ§a, Israel, JerusalÃ©m, Jesus, Julgamento, JustiÃ§a, Lei, Luz, Messias, Milagre, OraÃ§Ã£o, ParaÃ­so, PerdÃ£o, Profeta, RedenÃ§Ã£o, RessurreiÃ§Ã£o, Sabedoria, SalvaÃ§Ã£o, Santidade, SÃ¡bado, Templo, TentaÃ§Ã£o, Trindade, Verdade, Vontade). MitigaÃ§Ã£o A1 (conteÃºdo Ã© o bloco): mÃ­nimo 100 aceito, alvo 200-300. DefiniÃ§Ãµes descritivas, neutras, 1-3 frases.
    data/raw/themes.json: â‰¥10 temas {id, title, verses: [{book, chapter, verse, text}]} â€” texto do versÃ­culo EMBARCADO (D-11): amor, fÃ©, perdÃ£o, esperanÃ§a, oraÃ§Ã£o, salvaÃ§Ã£o, graÃ§a, sabedoria, perseveranÃ§a, paz. 5-8 versÃ­culos-chave cada (ex.: amor â†’ JoÃ£o 3:16, 1 CorÃ­ntios 13:4, 1 JoÃ£o 4:8...), texto extraÃ­do das traduÃ§Ãµes jÃ¡ embarcadas (tb ou alm1911) para consistÃªncia.
    Criar scripts/study-build.mjs (node, padrÃ£o generate-data): ler data/raw/dictionary.json e themes.json; validar shape (array nÃ£o vazio; entradas com word/definition strings; temas com id/title strings e verses toda com book/chapter/verse number + text string; throw com mensagem clara em shape invÃ¡lido); escrever public/data/study/dictionary.json + themes.json (cÃ³pias validadas); se raw ausente â†’ console.warn + skip (nÃ£o falhar build â€” permite 02-03 adicionar hymns/plans depois). Logar contagens.
    Criar src/lib/study.ts: export types DictionaryEntry { word: string; definition: string }, ThemeVerse { book: number; chapter: number; verse: number; text: string }, Theme { id: string; title: string; verses: ThemeVerse[] }; implementar getDictionary(): Promise<DictionaryEntry[]> e getThemes(): Promise<Theme[]> com fetch(`/data/study/${name}.json`) + cache em mÃ³dulo. Sem React/estado aqui â€” lib pura.
  </action>
  <verify>
    <automated>node scripts/study-build.mjs; node -e "const d=require('./public/data/study/dictionary.json'); if(!Array.isArray(d)||d.length<100||d.some(e=>typeof e.word!=='string'||typeof e.definition!=='string'||!e.word||!e.definition))process.exit(1)"; node -e "const t=require('./public/data/study/themes.json'); if(!Array.isArray(t)||t.length<10||t.some(x=>!x.id||!x.title||!Array.isArray(x.verses)||x.verses.some(v=>typeof v.book!=='number'||typeof v.chapter!=='number'||typeof v.verse!=='number'||typeof v.text!=='string')))process.exit(1)"; npm run lint; npx tsc --noEmit; npm run build (prebuild chain completo â€” search-build e study-build juntos)</automated>
  </verify>
  <done>DicionÃ¡rio (~100-300) e temas (â‰¥10 com texto embarcado) curados, validados e escritos em public/data/study/** (precached); getters com cache em study.ts.</done>
</task>

<task type="auto" reversibility="reversible">
  <name>Task 3: StudyView â€” abas DicionÃ¡rio e 'O que a BÃ­blia diz' + wiring no shell â€” EST-04, EST-06</name>
  <files>src/components/study-view.tsx, app/page.tsx</files>
  <read_first>
    - src/lib/study.ts (getDictionary/getThemes da Task 2)
    - src/lib/search-options.ts (normalizeTerm para o filtro do dicionÃ¡rio)
    - src/components/book-picker.tsx (padrÃ£o de card/abertura)
    - app/page.tsx (shell da Task 1 â€” onde plugar StudyView)
    - node_modules/next/dist/docs/ (regra Next.js 16 do AGENTS.md do projeto â€” ler o guia relevante antes de alterar app/page.tsx; heed deprecation notices)
  </read_first>
  <acceptance_criteria>
    - E2E: aba Estudo â†’ abas DicionÃ¡rio / O que a BÃ­blia diz renderizam; digitar "amor" no dicionÃ¡rio filtra para â‰¥1 verbete contendo amor
    - E2E: Temas â†’ abrir tema "Amor" â†’ lista versÃ­culos com texto embarcado exibido (abbrev cap:versÃ­culo + texto)
    - E2E: clique em um versÃ­culo do tema â†’ leitor abre com ?b=&c= (view muda para reader)
    - `npm run lint`/`npx tsc --noEmit`/`npm run build` passam; 0 erros console
  </acceptance_criteria>
  <action>
    Criar src/components/study-view.tsx ("use client"): props { index: BibleIndex; onNavigate: (bookId: number, chapter: number) => void }. Estado: aba "dicionario"|"temas" (default "dicionario"), query do dicionÃ¡rio, tema expandido. DicionÃ¡rio: carregar getDictionary() no mount; input aria-label "Buscar no dicionÃ¡rio" filtrando por normalizeTerm(query) contido em word ou definition; renderizar lista word + definition (texto puro). Temas: carregar getThemes(); card por tema (title) com clique expandindo (useState temaExpandido); versÃ­culos renderizados como `index.books[book].abbrev  chapter:verse` + text, clique â†’ onNavigate(book, chapter). Estados vazio/erro simples (texto neutro).
    app/page.tsx (Task 1): substituir o placeholder do tab "study" por <StudyView index={index} onNavigate={(b,c) => { setView("reader"); const url = new URL(window.location.href); url.searchParams.set("b", String(b)); url.searchParams.set("c", String(c)); history.replaceState(null, "", url.toString()); }} /> (Pitfall 4). Manter view reader como default.
  </action>
  <verify>
    <automated>npm run lint; npx tsc --noEmit; npm run build; E2E: fluxos DicionÃ¡rio (filtro) e Temas (expandir + navegar) com 0 erros console (padrÃ£o Fase 1)</automated>
  </verify>
  <done>Aba Estudo funcional com dicionÃ¡rio pesquisÃ¡vel e temas expansÃ­veis navegÃ¡veis; shell completo com 3 abas.</done>
</task>

</tasks>

## Flagged Assumptions (edge probe â€” specless fallback)

| Requirement | Edge | Resolution |
|-------------|------|------------|
| LEI-04 | Escopo da busca por traduÃ§Ã£o + custo de parse 2-4MB no celular | Resolvido explÃ­cito â€” Ã­ndice por traduÃ§Ã£o (Pitfall 7), getSearch lazy, SC2 <1s medido por performance.mark no E2E; tamanho medido/logado no build (OQ-2) |
| EST-04 | ConteÃºdo do dicionÃ¡rio nÃ£o existe antes da fase | Resolvido explÃ­cito (Task 2) + backstop registrado em must_haves.truths (verification: backstop): mÃ­n. 100 verbetes aceitos na mitigaÃ§Ã£o A1 |
| EST-06 | Texto dos versÃ­culos-chave deve estar offline | Resolvido explÃ­cito â€” texto embarcado no JSON (D-11), sem fetch ao abrir tema |

Breadcrumb canÃ´nico (canon-referral, sem mint): XSS via conteÃºdo curado â€” dicionÃ¡rio/temas renderizados como texto puro via React; sem dangerouslySetInnerHTML (verificaÃ§Ã£o grep na Task 1).

## Prohibitions

- Nunca marcar progresso de plano de leitura automaticamente â€” progresso sÃ³ muda por aÃ§Ã£o explÃ­cita do usuÃ¡rio (EST-05; implementado no 02-03 T3). Status: flagged â€” E2E deve provar que abrir/ler leitura nÃ£o marca dia.
- Nunca usar dangerouslySetInnerHTML â€” todo conteÃºdo (corpus, hinos, dicionÃ¡rio, anotaÃ§Ãµes, snippets de busca) Ã© renderizado como texto puro via React (XSS canÃ´nico, V5 ASVS). Status: flagged â€” grep no verify (`grep -rn "dangerouslySetInnerHTML" src/` deve retornar 0).

## Artifacts this phase produces

- `src/lib/search-options.ts` â€” `SEARCH_OPTIONS`, `SEARCH_QUERY_OPTS`, `normalizeTerm` (compartilhados build+client; espelho sincronizado em scripts/search-build.mjs â€” decisÃ£o registrada no SUMMARY)
- `scripts/search-build.mjs` â€” constrÃ³i `public/data/search/{tb,alm1911,blivre}.json` no build
- `src/lib/search.ts` â€” `getSearch(versionCode)`, `SearchResult`, self-heal (fetch + `putSearchIndex`)
- `src/components/search-view.tsx` â€” view de busca
- `src/components/study-view.tsx` â€” abas DicionÃ¡rio / O que a BÃ­blia diz
- `app/page.tsx` â€” shell com tab bar Leitura/Busca/Estudo
- `src/lib/study.ts` â€” `getDictionary()`, `getThemes()`, tipos DictionaryEntry/ThemeVerse/Theme
- `scripts/study-build.mjs` â€” validaÃ§Ã£o + escrita de `public/data/study/{dictionary,themes}.json` (extensÃ­vel no 02-03)
- Dados: `data/raw/dictionary.json` (~100-300), `data/raw/themes.json` (â‰¥10), `public/data/search/**`, `public/data/study/**`

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| clientâ†’/data/search + /data/study | EstÃ¡ticos first-party gerados no build (MiniSearch serializado, conteÃºdo curado) |
| clientâ†’IDB store search | Ãndice baixado persistido para offline (escritas via putSearchIndex â€” conteÃºdo first-party) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-02-01 | Tampering | Snippet de resultado em search-view | medium | mitigate | <mark> via split de texto React; proibido dangerouslySetInnerHTML (V5 ASVS); grep no verify |
| T-02-02-02 | Tampering | ConteÃºdo curado (dictionary/themes) | medium | mitigate | Renderizado como texto puro; dados de entrada validados por shape no study-build.mjs antes de embarcar |
| T-02-02-03 | DoS | Parse de Ã­ndice 2-4MB no app | low | mitigate | Carregado lazy sob demanda (sÃ³ ao abrir Busca), cache memÃ³ria+IDB; tamanho medido no build (OQ-2) |
| T-02-02-SC | Tampering | npm minisearch | high (verified OK) | mitigate | Audit em RESEARCH OK/Approved; pin ^7.2.0; sem checkpoint |
</threat_model>

<verification>
- `npm run lint`, `npx tsc --noEmit`, `npm run build` â€” PASS apÃ³s cada task (build full sÃ³ na Task 2 em diante, quando o chain prebuild estÃ¡ completo)
- AsserÃ§Ãµes node: search/{tb,alm1911}.json â‰¥31k docs; dictionary â‰¥100; themes â‰¥10 com verses vÃ¡lidas
- E2E: busca "amor" <1s + <mark> + navegaÃ§Ã£o; dicionÃ¡rio filtra; temas expandem e navegam; 0 erros console
</verification>

<success_criteria>
- SC2: busca <1s offline sobre ~31k versÃ­culos â€” VERIFICADO por E2E performance.mark (mÃ©trica no desktop; ace no mobile Ã© backstop)
- EST-04: dicionÃ¡rio integrado offline â€” VERIFICADO por node assert + E2E
- EST-06: "O que a BÃ­blia diz" com texto embarcado â€” VERIFICADO por E2E
- D-04/D-05: Ã­ndice prÃ©-computado por traduÃ§Ã£o, resultado nÃ­vel versÃ­culo, destaque, navegaÃ§Ã£o ?b=&c= â€” VERIFICADO por E2E + node assert
</success_criteria>

<output>
Create `.planning/phases/02-estudo-e-personaliza-o/02-02-SUMMARY.md` when done
</output>

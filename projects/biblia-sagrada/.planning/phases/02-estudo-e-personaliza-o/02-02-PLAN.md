---
phase: 02-estudo-e-personaliza-o
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - package.json
  - scripts/search-build.mjs
  - scripts/study-build.mjs
  - data/raw/dictionary.json
  - data/raw/themes.json
  - src/lib/search-options.ts
  - src/lib/search.ts
  - src/lib/study.ts
  - src/components/search-view.tsx
  - src/components/study-view.tsx
  - app/page.tsx
autonomous: true
requirements: [LEI-04, EST-04, EST-06]
estimate:
  tokens: 65000
  raw_tokens: 65000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Usuário busca palavra e encontra versículos em <1s, offline, com índice FTS pré-computado por tradução (MiniSearch 7.2.0) [LEI-04, D-04, SC2]"
    - "Resultado da busca é nível versículo com contexto e destaque (<mark>), e o clique navega para o capítulo via ?b=&c=&v= [D-05]"
    - "Busca é escopada por tradução — índice por versão; BLIVRE só pesquisável após baixada (ou online) [D-05, Pitfall 7]"
    - "Usuário consulta dicionário bíblico integrado, offline, com ~100-300 verbetes curados [EST-04, D-08, A1]"
    - "Usuário acessa a seção 'O que a Bíblia diz' com temas e versículos-chave com texto embarcado [EST-06, D-11]"
    - statement: "Conteúdo de estudo (dicionário ~100+ verbetes, temas ≥10) é produzido e curado durante esta fase — dados de entrada em data/raw/** validados e versionados pelo pipeline [A1/OQ-1]"
      verification: backstop
  artifacts:
    - path: "src/lib/search-options.ts"
      provides: "SEARCH_OPTIONS (MiniSearch) + normalizeTerm — fonte canônica, espelhada no build (Pitfall 1)"
      contains: "processTerm"
    - path: "scripts/search-build.mjs"
      provides: "Constrói public/data/search/{code}.json por tradução no build (2-4MB cada), com log de tamanho (OQ-2)"
      contains: "MiniSearch"
    - path: "src/lib/search.ts"
      provides: "getSearch(versionCode) lazy (memória → IDB search store → fetch) + SearchResult + self-heal putSearchIndex"
      contains: "getSearchIndex"
    - path: "src/components/search-view.tsx"
      provides: "View de busca: input, filtro de tradução, resultados com <mark>, clique → navega"
      min_lines: 40
    - path: "src/components/study-view.tsx"
      provides: "View Estudo: abas Dicionário e 'O que a Bíblia diz' (expansível no plano 02-03)"
      min_lines: 40
    - path: "app/page.tsx"
      provides: "Shell single-page: tab bar Leitura/Busca/Estudo (sem novas rotas)"
      contains: "Busca"
    - path: "scripts/study-build.mjs"
      provides: "Valida e escreve public/data/study/dictionary.json + themes.json (extensível no 02-03 p/ hinos e planos)"
      contains: "dictionary"
    - path: "src/lib/study.ts"
      provides: "Types + getDictionary()/getThemes() (fetch cache); estendido no 02-03 com getHymns/getPlans"
      contains: "getDictionary"
    - path: "data/raw/dictionary.json"
      provides: "~100-300 verbetes {word, definition}"
      min_lines: 100
    - path: "data/raw/themes.json"
      provides: "≥10 temas {id, title, verses:[{book,chapter,verse,text}]}"
      min_lines: 20
    - path: "public/data/search/tb.json"
      provides: "Índice MiniSearch TB (~31k docs)"
      min_lines: 1
    - path: "public/data/search/alm1911.json"
      provides: "Índice MiniSearch ALM1911 (~31k docs)"
      min_lines: 1
  key_links:
    - from: "src/lib/search.ts"
      to: "src/lib/bible.ts"
      via: "getSearchIndex/putSearchIndex (accessors do store search criados no 02-01)"
      pattern: "getSearchIndex"
    - from: "scripts/search-build.mjs"
      to: "src/lib/search-options.ts"
      via: "mesmo SEARCH_OPTIONS no build e no client (Pitfall 1 — loadJSON exige as mesmas options); deep-equal verificado"
      pattern: "SEARCH_OPTIONS"
    - from: "src/lib/search.ts"
      to: "public/data/search/*.json"
      via: "MiniSearch.loadJSON lazy sob demanda, sem fetch de ~31k docs no boot"
      pattern: "loadJSON"
    - from: "src/components/search-view.tsx"
      to: "URL"
      via: "onNavigate(book, chapter, version) → ?b=&c=&v= via replaceState string (Pitfall 4)"
      pattern: "replaceState"
    - from: "scripts/study-build.mjs"
      to: "public/data/study/*.json"
      via: "valida data/raw/** e grava em public/data/study/** (precached — D-08 conteúdo curado embarcado)"
      pattern: "study"
---

<objective>
Busca FTS no navegador + dicionário integrado + seção "O que a Bíblia diz".

Purpose: SC2 (busca em <1s sobre ~31k versículos, offline), EST-04 e EST-06. Índice MiniSearch pré-computado no build (1 arquivo por tradução em /data/search/{code}.json — D-04), carregado lazy no client, e conteúdo de estudo curado em /data/study/*.json. Introduz o shell single-page com tab bar Leitura/Busca/Estudo (OQ-3) que o plano 02-03 estende.
Output: busca por versículo com destaque e navegação, dicionário pesquisável, temas com versículos-chave embarcados, shell com 3 abas.
</objective>

<execution_context>
@C:/Users/VCM37/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/VCM37/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-estudo-e-personaliza-o/02-CONTEXT.md
@.planning/phases/02-estudo-e-personaliza-o/02-RESEARCH.md
@src/lib/bible.ts
@src/lib/settings.ts
@src/components/reader.tsx
@src/components/book-picker.tsx
@app/page.tsx
@scripts/generate-data.mjs
@package.json
</context>

<tasks>

<task type="tracer" reversibility="costly">
  <name>Task 1: Busca FTS end-to-end — MiniSearch, índice pré-computado, shell com tab bar — LEI-04, D-04, D-05, SC2</name>
  <files>package.json, src/lib/search-options.ts, scripts/search-build.mjs, src/lib/search.ts, src/components/search-view.tsx, app/page.tsx</files>
  <read_first>
    - 02-RESEARCH.md (Decisão central 1 — MiniSearch; Pitfall 1 options idênticas; Pitfall 7 escopo por tradução)
    - package.json (scripts prebuild; sem dep de busca ainda)
    - scripts/generate-data.mjs (estrutura de versões/livros gerada por VERSIONS — formato de referência)
    - src/lib/bible.ts (getSearchIndex/putSearchIndex criados no 02-01; BibleIndex com versions)
    - app/page.tsx (hoje renderiza <Reader /> puro)
    - node_modules/next/dist/docs/ (regra Next.js 16 do AGENTS.md do projeto — "use client" e shell de página única em app/page.tsx; ler o guia relevante antes de codar; heed deprecation notices)
  </read_first>
  <acceptance_criteria>
    - package.json contém "minisearch": "^7.2.0" em dependencies; prebuild = "node scripts/generate-data.mjs && node scripts/search-build.mjs && node scripts/study-build.mjs"
    - `npm run build` (executado na Task 2, quando study-build.mjs existe) gera public/data/search/tb.json e public/data/search/alm1911.json; node -e afirma que cada um parseia e representa ≥ 31000 versículos indexados
    - E2E: aba Busca → digitar "amor" → resultados renderizados com <mark> e tempo medido <1000ms → clicar 1º resultado → leitor abre com ?b=&c= corretos (e ?v= se filtro de tradução ativo)
    - E2E: trocar filtro para ALM 1911 → resultados refletem o índice alm1911 (abbrev/label)
    - E2E: nenhum erro de console; sem dangerouslySetInnerHTML no código (grep)
    - `npm run lint`, `npx tsc --noEmit`, `npm run build` passam
  </acceptance_criteria>
  <action>
    Instalar: `npm install minisearch@^7.2.0` (Package Legitimacy Audit OK/Approved em 02-RESEARCH.md — sem checkpoint; pin ^7.2.0).
    package.json: prebuild = "node scripts/generate-data.mjs && node scripts/search-build.mjs && node scripts/study-build.mjs" (chain completo só vale quando os 3 scripts existirem; study-build é criado na Task 2 — por isso o build full da Task 1 roda somente após a Task 2).
    Criar src/lib/search-options.ts: export const SEARCH_OPTIONS = { fields: ["text"], storeFields: ["book","abbrev","chapter","verse","text"], idField: "id", processTerm: (t) => { const n = t.normalize("NFD").replace(/[\u0300-\u036f]/g, "").toLowerCase(); const STOP = new Set(["a","de","do","da","em","e","o","os","as","um","uma","que","para","dos","das","não","nao","se","com"]); if (STOP.has(n) || !n) return null; return n; } }; export const SEARCH_QUERY_OPTS = { prefix: true, fuzzy: 0.1, combineWith: "AND" }; export function normalizeTerm(t: string): string (mesmo NFD strip + lowercase, sem stopwords). O CLIENT usa esses exports (Pitfall 1 — MiniSearch.loadJSON exige as mesmas options usadas no addAll; options divergentes corrompem a busca).
    Criar scripts/search-build.mjs (node): duplicar SEARCH_OPTIONS localmente com o comentário "MANTENHA EM SINCRONIA COM src/lib/search-options.ts" (o projeto não tem loader TS para build scripts; a verificação abaixo confere igualdade via deep-equal das duas constantes). Ler public/data/index.json (versions + books); para cada version.code: se !fs.existsSync(public/data/{code}) → console.warn(`skip ${code}`) e continuar (blivre ainda não baixada — 02-01 Task 3 deixa 404-skip e o self-heal fecha); senão walk nos arquivos {abbrev}.json → para cada capítulo e versículo push doc { id: `${bookId}:${ch}:${v}`, book: bookId, abbrev: book.abbrev, chapter: ch, verse: v, text: verseText }; const ms = new MiniSearch(SEARCH_OPTIONS); ms.addAll(docs); fs.writeFileSync(public/data/search/{code}.json, JSON.stringify(ms)); console.log(`${code}: ${(bytes/1024).toFixed(1)}KB, ${docs.length} docs`) (OQ-2: medir e logar o tamanho).
    Criar src/lib/search.ts: import { SEARCH_OPTIONS, SEARCH_QUERY_OPTS } from "@/lib/search-options"; import { getSearchIndex, putSearchIndex } from "@/lib/bible"; const cache = new Map<string, MiniSearch>(); export interface SearchResult { id: string; score: number; match: Record<string, string[]>; book: number; abbrev: string; chapter: number; verse: number; text: string }; export async function getSearch(versionCode: string): Promise<MiniSearch> — ordem: (1) cache em memória; (2) await getSearchIndex(versionCode) do IDB → MiniSearch.loadJSON(JSON.parse(saved), SEARCH_OPTIONS) em try/catch; (3) senão fetch(`/data/search/${versionCode}.json`) → res.ok senão throw new Error(`Índice de busca indisponível: ${versionCode}`) → putSearchIndex(versionCode, json) e loadJSON. O passo 3 é o self-heal que cobre BLIVRE baixada em 02-01 sem índice: a primeira busca dispara download+persistência do índice.
    Criar src/components/search-view.tsx ("use client"): props { index: BibleIndex; activeVersion: string; onNavigate: (bookId: number, chapter: number, version: string) => void }. Estado: term, version (default activeVersion), results: SearchResult[], loading, searchTimeMs, error. Filtro de tradução: <select aria-label="Tradução"> com index.versions (label + shortLabel). Submit: const t0 = performance.now(); const ms = await getSearch(version); const res = ms.search(normalizeTerm(term), SEARCH_QUERY_OPTS); setSearchTimeMs(performance.now() - t0); renderizar até 100 resultados: `abbrev chapter:verse` + texto com <mark> nos termos (split por termo normalizado em JSX — nunca dangerouslySetInnerHTML). Clique → onNavigate(r.book, r.chapter, version). Estados: loading, vazio "Nenhum resultado", erro (ex.: blivre sem índice → "Baixe a tradução para buscar offline").
    app/page.tsx: converter para "use client" shell: estado view "reader"|"search"|"study" (default "reader"); tab bar fixa inferior (OQ-3) com 3 botões Leitura/Busca/Estudo com aria-current; container com padding-bottom para a barra; view "reader" → <Reader />; view "search" → <SearchView index={index} activeVersion={readVersion()} onNavigate={(b,c,v) => { setView("reader"); const url = new URL(window.location.href); url.searchParams.set("b", String(b)); url.searchParams.set("c", String(c)); if (v) url.searchParams.set("v", v); history.replaceState(null, "", url.toString()); }} /> (Pitfall 4 — sempre url.toString()); view "study" → placeholder "Em breve" (Task 3 substitui). Carregar index via getIndex() em useEffect (compartilhado entre views; Reader carrega o próprio, cache interno).
  </action>
  <verify>
    <automated>node scripts/generate-data.mjs; node scripts/search-build.mjs (rodar ANTES do E2E — o E2E da busca depende do índice public/data/search/{tb,alm1911}.json existir no disco); npm install minisearch@^7.2.0; node -e "const a=require('fs').readFileSync('scripts/search-build.mjs','utf8'),b=require('fs').readFileSync('src/lib/search-options.ts','utf8');const get=c=>{const m=c.match(/SEARCH_OPTIONS\s*=\s*(\{[\s\S]*?\})/);return eval('('+m[1]+')')};if(JSON.stringify(get(a))!==JSON.stringify(get(b)))process.exit(1)"; npm run lint; npx tsc --noEmit; E2E: busca "amor" → <mark> + tempo <1000ms + navegação ?b=&c=; grep -rn "dangerouslySetInnerHTML" src/ (0 ocorrências)</automated>
  </verify>
  <done>Busca FTS <1s offline com índice pré-computado por tradução, resultado por versículo com destaque e navegação para o capítulo; shell com tab bar Leitura/Busca/Estudo.</done>
</task>

<task type="auto" reversibility="reversible">
  <name>Task 2: Conteúdo curado — dicionário + 'O que a Bíblia diz' — EST-04, EST-06, D-08, D-11</name>
  <files>data/raw/dictionary.json, data/raw/themes.json, scripts/study-build.mjs, src/lib/study.ts</files>
  <read_first>
    - 02-CONTEXT.md D-08 (dicionário 200-300 verbetes, mitigação A1 ~100) e D-11 (temas com texto embarcado em JSON)
    - scripts/generate-data.mjs (padrão de validação/shape — replicar em study-build.mjs)
    - scripts/search-build.mjs (se já criado; senão ignorar — script novo)
  </read_first>
  <acceptance_criteria>
    - node -e exec: public/data/study/dictionary.json é array com ≥100 objetos {word: string, definition: string} — todas word/definition não vazias
    - node -e exec: public/data/study/themes.json é array com ≥10 objetos {id, title, verses: [{book, chapter, verse, text}]} — cada verse com 4 campos preenchidos
    - `npm run build` completo passa com prebuild chain (generate-data + search-build + study-build)
    - src/lib/study.ts exporta getDictionary()/getThemes() com cache; `npm run lint`/`npx tsc --noEmit` passam
  </acceptance_criteria>
  <action>
    data/raw/dictionary.json: curar ~100-300 entradas {word, definition} — nomes próprios e termos essenciais (Abraão, Adão, Aliança, Amor, Anjo, Apóstolo, Arrependimento, Batismo, Bênção, Céu, Cristo, Discípulo, Espírito Santo, Fé, Graça, Israel, Jerusalém, Jesus, Julgamento, Justiça, Lei, Luz, Messias, Milagre, Oração, Paraíso, Perdão, Profeta, Redenção, Ressurreição, Sabedoria, Salvação, Santidade, Sábado, Templo, Tentação, Trindade, Verdade, Vontade). Mitigação A1 (conteúdo é o bloco): mínimo 100 aceito, alvo 200-300. Definições descritivas, neutras, 1-3 frases.
    data/raw/themes.json: ≥10 temas {id, title, verses: [{book, chapter, verse, text}]} — texto do versículo EMBARCADO (D-11): amor, fé, perdão, esperança, oração, salvação, graça, sabedoria, perseverança, paz. 5-8 versículos-chave cada (ex.: amor → João 3:16, 1 Coríntios 13:4, 1 João 4:8...), texto extraído das traduções já embarcadas (tb ou alm1911) para consistência.
    Criar scripts/study-build.mjs (node, padrão generate-data): ler data/raw/dictionary.json e themes.json; validar shape (array não vazio; entradas com word/definition strings; temas com id/title strings e verses toda com book/chapter/verse number + text string; throw com mensagem clara em shape inválido); escrever public/data/study/dictionary.json + themes.json (cópias validadas); se raw ausente → console.warn + skip (não falhar build — permite 02-03 adicionar hymns/plans depois). Logar contagens.
    Criar src/lib/study.ts: export types DictionaryEntry { word: string; definition: string }, ThemeVerse { book: number; chapter: number; verse: number; text: string }, Theme { id: string; title: string; verses: ThemeVerse[] }; implementar getDictionary(): Promise<DictionaryEntry[]> e getThemes(): Promise<Theme[]> com fetch(`/data/study/${name}.json`) + cache em módulo. Sem React/estado aqui — lib pura.
  </action>
  <verify>
    <automated>node scripts/study-build.mjs; node -e "const d=require('./public/data/study/dictionary.json'); if(!Array.isArray(d)||d.length<100||d.some(e=>typeof e.word!=='string'||typeof e.definition!=='string'||!e.word||!e.definition))process.exit(1)"; node -e "const t=require('./public/data/study/themes.json'); if(!Array.isArray(t)||t.length<10||t.some(x=>!x.id||!x.title||!Array.isArray(x.verses)||x.verses.some(v=>typeof v.book!=='number'||typeof v.chapter!=='number'||typeof v.verse!=='number'||typeof v.text!=='string')))process.exit(1)"; npm run lint; npx tsc --noEmit; npm run build (prebuild chain completo — search-build e study-build juntos)</automated>
  </verify>
  <done>Dicionário (~100-300) e temas (≥10 com texto embarcado) curados, validados e escritos em public/data/study/** (precached); getters com cache em study.ts.</done>
</task>

<task type="auto" reversibility="reversible">
  <name>Task 3: StudyView — abas Dicionário e 'O que a Bíblia diz' + wiring no shell — EST-04, EST-06</name>
  <files>src/components/study-view.tsx, app/page.tsx</files>
  <read_first>
    - src/lib/study.ts (getDictionary/getThemes da Task 2)
    - src/lib/search-options.ts (normalizeTerm para o filtro do dicionário)
    - src/components/book-picker.tsx (padrão de card/abertura)
    - app/page.tsx (shell da Task 1 — onde plugar StudyView)
    - node_modules/next/dist/docs/ (regra Next.js 16 do AGENTS.md do projeto — ler o guia relevante antes de alterar app/page.tsx; heed deprecation notices)
  </read_first>
  <acceptance_criteria>
    - E2E: aba Estudo → abas Dicionário / O que a Bíblia diz renderizam; digitar "amor" no dicionário filtra para ≥1 verbete contendo amor
    - E2E: Temas → abrir tema "Amor" → lista versículos com texto embarcado exibido (abbrev cap:versículo + texto)
    - E2E: clique em um versículo do tema → leitor abre com ?b=&c= (view muda para reader)
    - `npm run lint`/`npx tsc --noEmit`/`npm run build` passam; 0 erros console
  </acceptance_criteria>
  <action>
    Criar src/components/study-view.tsx ("use client"): props { index: BibleIndex; onNavigate: (bookId: number, chapter: number) => void }. Estado: aba "dicionario"|"temas" (default "dicionario"), query do dicionário, tema expandido. Dicionário: carregar getDictionary() no mount; input aria-label "Buscar no dicionário" filtrando por normalizeTerm(query) contido em word ou definition; renderizar lista word + definition (texto puro). Temas: carregar getThemes(); card por tema (title) com clique expandindo (useState temaExpandido); versículos renderizados como `index.books[book].abbrev  chapter:verse` + text, clique → onNavigate(book, chapter). Estados vazio/erro simples (texto neutro).
    app/page.tsx (Task 1): substituir o placeholder do tab "study" por <StudyView index={index} onNavigate={(b,c) => { setView("reader"); const url = new URL(window.location.href); url.searchParams.set("b", String(b)); url.searchParams.set("c", String(c)); history.replaceState(null, "", url.toString()); }} /> (Pitfall 4). Manter view reader como default.
  </action>
  <verify>
    <automated>npm run lint; npx tsc --noEmit; npm run build; E2E: fluxos Dicionário (filtro) e Temas (expandir + navegar) com 0 erros console (padrão Fase 1)</automated>
  </verify>
  <done>Aba Estudo funcional com dicionário pesquisável e temas expansíveis navegáveis; shell completo com 3 abas.</done>
</task>

</tasks>

## Flagged Assumptions (edge probe — specless fallback)

| Requirement | Edge | Resolution |
|-------------|------|------------|
| LEI-04 | Escopo da busca por tradução + custo de parse 2-4MB no celular | Resolvido explícito — índice por tradução (Pitfall 7), getSearch lazy, SC2 <1s medido por performance.mark no E2E; tamanho medido/logado no build (OQ-2) |
| EST-04 | Conteúdo do dicionário não existe antes da fase | Resolvido explícito (Task 2) + backstop registrado em must_haves.truths (verification: backstop): mín. 100 verbetes aceitos na mitigação A1 |
| EST-06 | Texto dos versículos-chave deve estar offline | Resolvido explícito — texto embarcado no JSON (D-11), sem fetch ao abrir tema |

Breadcrumb canônico (canon-referral, sem mint): XSS via conteúdo curado — dicionário/temas renderizados como texto puro via React; sem dangerouslySetInnerHTML (verificação grep na Task 1).

## Prohibitions

- Nunca marcar progresso de plano de leitura automaticamente — progresso só muda por ação explícita do usuário (EST-05; implementado no 02-03 T3). Status: flagged — E2E deve provar que abrir/ler leitura não marca dia.
- Nunca usar dangerouslySetInnerHTML — todo conteúdo (corpus, hinos, dicionário, anotações, snippets de busca) é renderizado como texto puro via React (XSS canônico, V5 ASVS). Status: flagged — grep no verify (`grep -rn "dangerouslySetInnerHTML" src/` deve retornar 0).

## Artifacts this phase produces

- `src/lib/search-options.ts` — `SEARCH_OPTIONS`, `SEARCH_QUERY_OPTS`, `normalizeTerm` (compartilhados build+client; espelho sincronizado em scripts/search-build.mjs — decisão registrada no SUMMARY)
- `scripts/search-build.mjs` — constrói `public/data/search/{tb,alm1911,blivre}.json` no build
- `src/lib/search.ts` — `getSearch(versionCode)`, `SearchResult`, self-heal (fetch + `putSearchIndex`)
- `src/components/search-view.tsx` — view de busca
- `src/components/study-view.tsx` — abas Dicionário / O que a Bíblia diz
- `app/page.tsx` — shell com tab bar Leitura/Busca/Estudo
- `src/lib/study.ts` — `getDictionary()`, `getThemes()`, tipos DictionaryEntry/ThemeVerse/Theme
- `scripts/study-build.mjs` — validação + escrita de `public/data/study/{dictionary,themes}.json` (extensível no 02-03)
- Dados: `data/raw/dictionary.json` (~100-300), `data/raw/themes.json` (≥10), `public/data/search/**`, `public/data/study/**`

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→/data/search + /data/study | Estáticos first-party gerados no build (MiniSearch serializado, conteúdo curado) |
| client→IDB store search | Índice baixado persistido para offline (escritas via putSearchIndex — conteúdo first-party) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-02-01 | Tampering | Snippet de resultado em search-view | medium | mitigate | <mark> via split de texto React; proibido dangerouslySetInnerHTML (V5 ASVS); grep no verify |
| T-02-02-02 | Tampering | Conteúdo curado (dictionary/themes) | medium | mitigate | Renderizado como texto puro; dados de entrada validados por shape no study-build.mjs antes de embarcar |
| T-02-02-03 | DoS | Parse de índice 2-4MB no app | low | mitigate | Carregado lazy sob demanda (só ao abrir Busca), cache memória+IDB; tamanho medido no build (OQ-2) |
| T-02-02-SC | Tampering | npm minisearch | high (verified OK) | mitigate | Audit em RESEARCH OK/Approved; pin ^7.2.0; sem checkpoint |
</threat_model>

<verification>
- `npm run lint`, `npx tsc --noEmit`, `npm run build` — PASS após cada task (build full só na Task 2 em diante, quando o chain prebuild está completo)
- Asserções node: search/{tb,alm1911}.json ≥31k docs; dictionary ≥100; themes ≥10 com verses válidas
- E2E: busca "amor" <1s + <mark> + navegação; dicionário filtra; temas expandem e navegam; 0 erros console
</verification>

<success_criteria>
- SC2: busca <1s offline sobre ~31k versículos — VERIFICADO por E2E performance.mark (métrica no desktop; ace no mobile é backstop)
- EST-04: dicionário integrado offline — VERIFICADO por node assert + E2E
- EST-06: "O que a Bíblia diz" com texto embarcado — VERIFICADO por E2E
- D-04/D-05: índice pré-computado por tradução, resultado nível versículo, destaque, navegação ?b=&c= — VERIFICADO por E2E + node assert
</success_criteria>

<output>
Create `.planning/phases/02-estudo-e-personaliza-o/02-02-SUMMARY.md` when done
</output>

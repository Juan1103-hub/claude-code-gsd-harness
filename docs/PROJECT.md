# PROJECT.md — claude-code-gsd-harness

Este é o harness em si (template), não um projeto de produto. Preencha este arquivo apenas se o próprio harness ganhar features (ex: scripts de setup). Projetos reais têm seu próprio `docs/PROJECT.md` dentro de `projects/<nome>/`.
